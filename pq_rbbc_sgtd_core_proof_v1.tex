\documentclass[11pt]{article}
\usepackage[a4paper,margin=25mm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\newtheorem{definition}{Definition}[section]
\newtheorem{theorem}[definition]{Theorem}
\newtheorem{lemma}[definition]{Lemma}
\newtheorem{proposition}[definition]{Proposition}
\newtheorem{corollary}[definition]{Corollary}
\newtheorem{assumption}[definition]{Assumption}
\newtheorem{remark}[definition]{Remark}

\newcommand{\Core}{\mathsf{Core}}
\newcommand{\BUOV}{\mathsf{BUOV}}
\newcommand{\RBBC}{\mathsf{RBBC}}
\newcommand{\Piiss}{\Pi_{\mathsf{issue}}}
\newcommand{\TEnc}{\mathsf{TEnc}}
\newcommand{\HDEM}{\mathsf{HDEM}}
\newcommand{\TPDec}{\mathsf{TPDec}}
\newcommand{\TComb}{\mathsf{TComb}}
\newcommand{\Verify}{\mathsf{Verify}}
\newcommand{\negl}{\mathsf{negl}}
\newcommand{\Adv}{\mathsf{Adv}}
\newcommand{\QPT}{\mathsf{QPT}}
\newcommand{\Enc}{\mathsf{Enc}}
\newcommand{\Dec}{\mathsf{Dec}}
\newcommand{\Hash}{\mathsf{H}}
\newcommand{\rid}{\mathsf{rid}}
\newcommand{\sn}{\mathsf{sn}}
\newcommand{\ctx}{\mathsf{ctx}}
\newcommand{\pp}{\mathsf{pp}}
\newcommand{\pk}{\mathsf{pk}}
\newcommand{\sk}{\mathsf{sk}}
\newcommand{\eps}{\varepsilon}

\title{A Post-Quantum Relation-Bound Blind Certification Core\\
with Signature-Gated Threshold Decryption}
\author{Research construction draft, version 2.10}
\date{25 August 2026}

\begin{document}
\maketitle

\begin{abstract}
This document specifies and proves an abstract post-quantum cryptographic core for
privacy-preserving, accountable credentials.  During an offline issuance phase, a
holder obtains a blind signature only after proving that the hidden ticket, the
blind-signing request, the holder secret, and a trace ciphertext are mutually
consistent.  During opening, threshold decryption shares are released only for a
validly signed ticket and an independently authorized case.  This
\emph{signature-gated threshold decryption} rule replaces a public validity proof
carried by every online ticket.

The main result is conditional and modular: assuming simulation-extractable
zero knowledge, one-more unforgeability and blindness of the selected
blind-signature component, and a compact threshold encryption scheme with post-quantum
privacy and unique openings, the construction provides issuer unlinkability,
certified ciphertext validity, decoder safety, trace correctness,
non-frameability, and a gated chosen-ciphertext privacy notion against fewer than
the threshold number of opening authorities.  The result deliberately does not
claim ordinary IND-CCA security for the bare trace encryption.  Version 1.2
also instantiates the trace relation as a Niederreiter syndrome plus a
SHAKE256/KMAC256 one-time hash DEM, proves information-theoretic uniqueness of
the decoded error, and removes a redundant ticket commitment.  Version 1.3
freezes a paper-compatible Blind-UOV-Is/Shorter costing profile, fixes a
368-byte payload, gives a reproducible five-block circuit count, and separates
the large issuance NIZK from the CAP proof carried by the final Blind-UOV
signature.  Version 1.4 adds an executable witness builder, a streaming
characteristic-two circuit generator, independent SHAKE256/KMAC256 checks, and
full-circuit rejection tests.  Native Blind-UOV circuit integration, backend
simulation extractability, and the threshold implementation remain explicit
obligations.  Version 1.5 lowers the complete incremental relation into a
checksummed binary $\mathbb F_2$-R1CS archive, independently parses every row,
and separates the nonlinear-only cost model from the portable materialized-row
count.  Version 1.6 corrected the signer-view ABI but incorrectly treated two
independently openable 256-bit request lanes as a 512-bit random-function
output.  Version 1.7 removes that unsound amplification.  It selects one
Blind-UOV-III/Shorter instance with a 576-bit request target and derives
cross-message request binding in the QROM from CAP unique-witness soundness and
the collision bound for a shifted 576-bit random function.  The cached issuer
key grows to 189.2~KB, while the online ticket remains 12,012 bytes.  Native
Blind-UOV circuit integration and proof-backend qualification remain explicit
obligations.  Version 1.8 writes $h_{\BUOV}=H_{\BUOV}(m,c_r)$ and internalizes
the linear request equation $y=r+h_{\BUOV}$ as 576 binary equalities.  It
narrows the sole external boundary to
$h_{\BUOV}=H_{\BUOV}(m,\mathsf{CAP.Commit}(r;\rho))$ and adds a fail-closed
native-import contract.  It also freezes the paper's NIST-III/Shorter TCitH
tree parameters and exposes the required $\mathbb F_{2^{193}}$ backend lift.
Version 1.9 implements polynomial-basis arithmetic for that field and a
source-pinned eight-element Anemoi permutation as ordinary native rank-one
rows.  The implementation also exposes a reproducibility blocker rather than
hiding it: Blind-UOV reports 240 constraints per level-III permutation, while
the public upstream round rule and the published characteristic-two rule give
336 and 360 nonlinear rows, respectively, under the direct closed-Flystel
lowering.  The full CAP-hash import therefore remains fail-closed.
Version 2.0 deliberately selects the aggressive interoperability fork.  It
freezes an independent profile named
\emph{PQ-RBBC-Anemoi-193/336-Sponge-v1}, with four rate and four capacity
elements, injective length framing, explicit domain separation, and a 576-bit
request-binding output $H_{\RBBC}$.  The native trace constrains input bitness,
byte-to-field packing, two permutation calls for the frozen request vector,
state chaining, and output decomposition.  This removes the 240-row artifact
gap as an engineering blocker by giving up bit-exact Blind-UOV compatibility;
it does not inherit the paper's concrete security or 11,644-byte signature
claim.  At the version-2.0 checkpoint, CAP.Commit and GGM remained open;
fork-specific security proof and fresh benchmarking still remain open and
production stays fail-closed.
Version 2.1 implements a source-grounded direct reference for Protocols
8--10 of Blind-UOV: salted GGM seed derivation, leaf commitments and tapes,
random degree-one polynomial coefficients, cross-repetition corrections,
the consistency digest, canonical commitment serialization, and the exact
byte-level join to $H_{\RBBC}$.  A reduced non-secure vector executes all code
paths and mutation tests.  The production profile has 18 trees, 40,960 leaves,
a 5,391-byte hidden commitment, and an exact estimate of 389,974 selected
Anemoi permutations, or 131,031,264 nonlinear permutation rows.  The production
vector and native inter-call row stream have not been materialized, so the
parent archive deliberately retains one external assertion and production
closure remains false.
Version 2.2 lowers the complete reduced CAP fixture and its final
$H_{\RBBC}(m,c_r)$ join into one zero-callback native
$\mathbb F_{2^{193}}$ row stream.  The frozen component has 88,282 rows,
59,602 wires, 24 XOF calls, 57 Anemoi permutations, witness-independent
topology, and zero external assertions.  Input, commitment, and request-hash
mutation tests reject stale witnesses.  This is an explicitly non-secure
64-bit fixture: it validates the lowering and wire-identity architecture but
does not close the 18-tree production profile.
Version 2.3 removes the one-rate-block limitation from the native sponge.  A
frozen 2,450-bit tape trace uses four squeeze blocks, three constrained
squeeze-state permutations, 4,858 rows, and 4,845 wires.  The same primitive
is integrated into an extended CAP fixture in which all eight leaf tapes have
the production width.  That zero-callback component has 113,802 rows, 85,034
wires, 24 XOF calls, 81 Anemoi permutations, witness-independent topology,
and zero external assertions.  The extended fixture deliberately retains the
64-bit one-coefficient witness and two tiny trees.  It closes only the
multi-squeeze engineering blocker; production multi-coefficient polynomial
hashing, the 18-tree execution, and fork-specific security qualification remain
open.
Version 2.4 closes the independent field-arithmetic blocker.  A bit-bound
$\mathbb F_{2^{193}}$ multiplication fixture uses one native multiplication
row, and a frozen 2,048-bit, 11-coefficient Horner fixture evaluates at two
constrained nonzero distinct points with 20 multiplication rows and zero
external assertions.  The same generic lowering is integrated with all
symbolic extension-mask slices in a deliberately non-secure 386-bit,
two-coefficient CAP fixture whose tapes remain 2,450 bits.  That combined
component has 125,401 rows, 92,816 wires, 14 Horner multiplication rows,
witness-independent topology, and zero external assertions.  The full
2,048-bit, 18-tree production integration, fork-specific security proof, and
fresh benchmarks remain open; the parent archive therefore still retains one
external assertion.
Version 2.5 closes the next production-shape engineering boundary.  One real
$2^{11}$-leaf tree is executed with the complete 2,048-bit witness, eleven
field coefficients, degree-12 extension masks, two consistency points, and
2,450-bit tapes.  A bounded-memory lowering hashes 26,126,283 canonical rows
and 19,903,324 wire identifiers without retaining the 18,869,935,441-byte
expanded stream.  The component has zero callbacks and zero external
assertions; its peak resident set was 140,932~KiB.  This is a non-secure
one-tree topology result, not a complete satisfiability archive: the full
assignment, the $2^{12}$-leaf shard, the other 17 trees, the parent wire join,
fork-specific security proof, and fresh proof benchmarks remain open.
Version 2.6 closes the assignment boundary for that shard.  It writes every
one of the 19,903,324 field-wire values as a canonical 25-byte element in a
497,583,228-byte fixed-width archive, verifies the archive digest, and replays
all 26,126,283 rows through an mmap-backed assignment evaluator with zero
failures.  Five exact stale-witness probes covering GGM source binding, tape
packing, Horner multiplication, commitment publication, and request binding
are rejected.  This still does not close the $2^{12}$-leaf shard, all eighteen
trees, the parent wire join, the fork-specific security proof, or a
post-quantum proof backend.
Version 2.7 closes the second production tree shape separately.  One real
$2^{12}$-leaf tree uses the safe degree-13 extension, because degree 12 has
only 4,095 nonzero elements.  Its canonical assignment contains 39,789,564
field wires in a 994,739,228-byte archive.  An mmap replay accepts all
52,224,501 rows with zero failures, and five corresponding stale-witness
probes reject.  Both constituent production tree shapes now have complete
assignment evidence, but the 16-plus-2 tree composition, cross-tree
corrections, exact parent wire join, fork-specific reductions, and proof
backend remain open.
Version 2.8 executes the actual mixed 18-tree production reference for the
first time.  In canonical profile order, two 4,096-leaf degree-13 trees precede
sixteen 2,048-leaf degree-12 trees.  A linked composition document freezes all
122,847 ordered CAP XOF calls, 17 cross-tree correction pairs, the complete
5,391-byte commitment, the request-binding hash, and the exact previously
verified shard evidence selected at every tree position.  Five stale-document
mutations reject.  This closes the full reference vector and canonical link
schedule, not a monolithic native assignment: the shared global transcript
tail, exact cross-segment wire identities, parent join, reductions, and proof
backend remain open.
Version 2.9 closes the shared-tail part of that obligation as an independent
native relation.  Bit-constrained ports for all eighteen tree outputs feed one
set of correction equations, $H_1$, consistency points, shared $\alpha$, all
eighteen $\xi$ components, $H_2$, the canonical 5,391-byte commitment, and
the request-binding hash.  Its fixed-width assignment is replayed completely
with zero failures and six stale-witness probes reject.  This does not yet
materialize the eighteen tree-producer segments or their exact cross-segment
wire identities, so the complete composition and parent join remain open.
Version 2.10 implements the complementary producer relation on the reduced
two-tree fixture.  Each position-sensitive segment constrains GGM derivation,
leaf commitments and tapes, wide plain XORs, and point-dependent mask Horner
evaluation without duplicating $H_1$ or $H_2$.  Both assignments replay with
zero failures, twelve selected stale witnesses reject, and all eight producer
output ports match the version-2.9 tail consumer ABI by ID, width, and value
digest.  The consistency points remain explicit local inputs rather than
identical tail wires, and no production producer has yet been materialized.
\end{abstract}

\section{Status, scope, and non-goals}

This is the standalone cryptographic core.  It intentionally excludes satellite
AKE, radio transport, FLEO relay behavior, handover, revocation distribution, and
concrete parameter optimization.  Issuance and opening may use large proofs,
large public keys, and large private keys.  The constrained online path carries
only a ticket payload and one compact blind signature.

The construction is designed for the following division of trust.

\begin{itemize}[leftmargin=*]
  \item The issuing HNCC is \emph{honest but curious}: it authenticates the real
  identity, enforces a common issuance policy, verifies $\Piiss$, and signs only
  after acceptance, but later tries to link an observed ticket to an issuance
  session.
  \item Holders and network attackers may be malicious and quantum polynomial
  time.
  \item Fewer than $\tau$ of $n$ opening authorities may be corrupted.  Any
  authorized set of at least $\tau$ authorities can open a ticket.
  \item Issuer signing keys and opening decryption keys are independent even when
  the organizations operating them overlap.
  \item Per-user visible metadata is forbidden.  Version, epoch, policy,
  expiration bucket, and key identifiers are common to a sufficiently large
  anonymity set; otherwise they become an issuer watermark outside the
  cryptographic game.
\end{itemize}

\paragraph{Claim boundary.}
The theorems below are complete reductions for the abstract interfaces and games
defined here.  They are not a claim that the PQ-RBBC-BUOV-336 fork or threshold
Niederreiter papers already implement every required interface.  The forked
request equations and their visibility are fixed in
Section~\ref{sec:buov-normal}; the exact added
circuit is counted in Section~\ref{sec:circuit-audit} and executed in
Section~\ref{sec:executable-relation}.  The resulting proof-size and prover
runtime figures remain paper-anchored estimates rather than implementation
benchmarks.  The executable generator is a relation validator, not a proof
backend.  The numerical Niederreiter set is a size anchor, not a deployment
approval.

\section{Notation and post-quantum conventions}

The security parameter is $\lambda$.  All adversaries and reductions are
uniform or non-uniform $\QPT$ algorithms.  A function $\negl(\lambda)$ is
negligible.  Encodings are canonical, length-prefixed, and domain separated.
Concatenation is written $\|$.  Every hash invocation includes a fixed protocol
label and version.

The issuing organization authenticates a real identity $\rid$ before the
cryptographic issuance protocol.  The session identifier $\mathsf{sid}$ is used
only for replay and quota control and is never copied into a ticket.  The common
issuance information is
\[
  \mathsf{ci}=(\mathsf{version},\mathsf{epoch},\mathsf{domain},
  \mathsf{policy},\mathsf{expiryBucket},\mathsf{kid}_{\mathrm{OA}},
  \mathsf{kid}_{\mathrm{I}}).
\]
Its fixed 32-byte ticket representation is
\[
 \ctx=\mathsf{SHAKE256}(\mathtt{PQ\mbox{-}RBBC/CTX}\|
       \mathsf{Encode}(\mathsf{ci}),256).
\]
The federation configuration signature authenticates $(\mathsf{ci},\ctx)$.
The digest is computed once outside $\Piiss$ and is a public input; the issuer
cannot select a per-user value.

For any game $G$, $\Adv^G_{\mathcal A}(\lambda)$ denotes the usual absolute
distance from the ideal winning probability.  Oracle calls are polynomially
bounded.

\section{Required primitive interfaces}

\subsection{Hashing and CAP commitment}

We use domain-separated hashes
\[
  H_{\mathrm{ticket}},\quad H_{\RBBC},\quad H_{\mathrm{hold}},\quad
  H_{\mathrm{evidence}}.
\]
They are modeled as quantum-resistant collision-resistant hashes.  A random
oracle or QROM instantiation may be used by a concrete component, but the core
proof calls only the explicitly named property.

For the version-1.3 circuit audit, fix
\begin{align*}
 H_{\mathrm{ticket}}(X)&=\mathsf{SHAKE256}
   (\mathtt{PQ\mbox{-}RBBC/TICKET}\|X,256),\\
 H_{\mathrm{hold}}(k)&=\mathsf{SHAKE256}
   (\mathtt{PQ\mbox{-}RBBC/HOLD}\|k,256).
\end{align*}
The request-binding hash $H_{\RBBC}$ uses the independently frozen
PQ-RBBC-Anemoi-193/336 sponge profile.  Version 2.1 instantiates the CAP
commitment internals as an independent direct reference and canonical byte
encoding; its full native row stream is not yet instantiated.  The Blind-UOV
paper is a framework and size comparator, not a bit-exact implementation claim.
This mixed choice retains standardized
SHAKE/KMAC for ticket and trace logic while using the native-field sponge only
inside the forked blind-request boundary.

There is no separate public commitment to the ticket payload.  The accepting
proof extracts $M$, Equation~\eqref{eq:i2} fixes its digest, and the forked
request binds that digest.  Publishing an additional $c_M$ would add a primitive
and a circuit block without strengthening this chain.

\subsection{Forked BUOV request normal form and visibility}\label{sec:buov-normal}

From version 2.0 onward, the operational symbol $\BUOV$ denotes the independent
PQ-RBBC-BUOV-336 fork.  It retains the hidden-state request shape of
Blind-UOV Protocols 3 and 4, but replaces the unreproduced permutation/hash
instance.  References to the published Blind-UOV construction are explicit
comparisons only.  None of its concrete security or size results transfers
automatically to this fork.

The blind-signature interface is
\[
  (\mathsf{bsk},\mathsf{bvk})\leftarrow\BUOV.\mathsf{KeyGen}(1^\lambda),
\]
with an interactive request/sign/unblind protocol and a public verification
algorithm
\[
  \BUOV.\mathsf{Vfy}(\mathsf{bvk},m,\sigma).
\]

The core needs more than ordinary blindness: the issuance proof must identify
the exact hidden message represented by the request.  Protocols 3 and 4 of the
31 October 2025 revision have the user compute~\cite{blind-uov}
\begin{align}
 c_r&\leftarrow \mathsf{CAP.Commit}(r;\rho),
   \tag{B1}\label{eq:b1}\\
 y&=r+H_{\RBBC}(m,c_r)\in\mathbb F_q^o.
   \tag{B2}\label{eq:b2}
\end{align}
Only $(y,\pi_1)$ is sent to the signer.  The values $m,r,\rho,c_r$ are
witness/state, not signer-visible request fields.  The final signature later
contains $(c_r,c_x,\pi_2)$.  Publishing $c_r$ during issuance would both depart
from the paper and give the issuer a direct link to the final signature.

A Blind-UOV-Is target is only 256 bits.  Our authorization-binding property is
a freely chosen cross-message collision problem, not a fixed-target preimage
problem, so its generic quantum scale is only about
$2^{256/3}=2^{85}$~\cite{bht,compressed-oracle}.  Version 1.7 instead selects
the paper's Blind-UOV-III/Shorter profile: $r$ and $y$ are 576 bits, the cached
public key is 189.2~KB, and the final signature is 11,644 bytes
\cite{blind-uov}.  Version 2.0 keeps the 576-bit interface but treats those
key and signature figures as provisional engineering targets, not measurements
or theorems for the fork.  Define
\[
 J_m(r,\rho)=r+H_{\RBBC}
   (m,\mathsf{CAP.Commit}(r;\rho)).
\]
The public request is exactly the 72-byte value $\beta=y$; its hidden witness
is $b=(r,\rho)$.

\begin{definition}[PQ-RBBC-BUOV-336 request relation]\label{def:rrbs}
For $\beta=y$ and $b=(r,\rho)$, define
\[
 R^{\RBBC\text{-}336}_{\mathsf{req}}(y;m,r,\rho)=1
 \Longleftrightarrow
 c_r=\mathsf{CAP.Commit}(r;\rho)\ \wedge\
 y=r+H_{\RBBC}(m,c_r),
\]
where $c_r$ is a derived hidden witness.  A completed session is intended to
return one PQ-RBBC-BUOV-336 fork signature
$\sigma=(c_r,c_x,\pi_2)$ on $m$; its concrete format and size require fresh
validation.
\end{definition}

\begin{theorem}[QROM cross-message request binding]\label{thm:j-qrom}
Let $H_{\RBBC}:\{0,1\}^*\rightarrow\{0,1\}^{576}$ be an independent quantum
random oracle.  Assume the CAP commitment has straightline extraction and a
unique committed mask: producing one admissible $c_r$ with two different
extracted $r$ values is negligible.  If an adversary makes at most $q$ relevant
quantum-oracle/evaluation queries, then
\[
 \Adv^{\mathsf{xmsg}}_{J}(\mathcal A)
 \leq
 \Adv^{\mathsf{uw}}_{\mathsf{CAP}}(\mathcal B_1)
 +\Adv^{\mathsf{ext}}_{\mathsf{CAP}}(\mathcal B_2)
 +O\!\left(\frac{q^3}{2^{576}}\right).
\]
In particular, the generic quantum collision scale is $2^{576/3}=2^{192}$;
at $q=2^{128}$ the random-function term is $O(2^{-192})$.
\end{theorem}

\begin{proof}
Run the straightline CAP extractor on every admissible commitment relevant to
the adversary's output.  Abort on extraction failure.  If one commitment yields
two different masks, output a CAP unique-witness violation.  Otherwise each
admissible commitment $c$ has a well-defined value $r(c)$ fixed before the
domain-separated query $H_{\RBBC}(m,c)$.  Quotienting duplicate openings with
the same $(c,r(c))$, define
\[
 Z(m,c)=H_{\RBBC}(m,c)+r(c).
\]
For every admissible input, addition of the fixed $r(c)$ is a permutation of
the 576-bit oracle range.  Thus $Z$ is a pointwise-shifted 576-bit random
function.  If the adversary gives two accepted openings of one public $y$ for
$m\neq m'$, then $(m,c)\neq(m',c')$ and
$Z(m,c)=Z(m',c')=y$, a collision.  The compressed-oracle collision bound is
$O(q^3/2^{576})$~\cite{compressed-oracle}; the stated CAP bad events cover the
remaining cases.
\end{proof}

The theorem is an ideal-QROM statement.  Version 2.0 supplies a concrete
Anemoi-based function with the same 576-bit range, but does not prove that this
function realizes an independent quantum random oracle.  Fork qualification
must therefore include cryptanalysis and a concrete-to-ideal justification or
must retain QROM programmability as an explicit heuristic assumption.

\begin{remark}[Why v1.6 parallel repetition was removed]
Two independently openable 256-bit lanes do not behave as one monolithic
512-bit random function.  After fixing $m,m'$, an attacker can run a BHT claw
search for lane 0 and then another for lane 1, for total work about
$2\cdot2^{85}$ rather than $2^{170}$.  A larger single request range gives the
required collision exponent without adding a correlation assumption that might
damage blindness.
\end{remark}

\begin{remark}[Why ordinary blind signing is insufficient]
If $\Piiss$ proves a valid ciphertext but the proof is not tied to the exact
blind request $\beta$, a malicious holder can prove statement $M_0$ and ask for
a signature on $M_1$.  The
$R^{\BUOV\text{-}III}_{\mathsf{req}}$ conjunct is the formal
connection that prevents this substitution.
\end{remark}

\subsection{Simulation-extractable NIZK argument of knowledge}

The proof system $\Piiss=(\mathsf{Setup},\mathsf{Prove},\mathsf{Vfy})$ is a
post-quantum simulation-extractable NIZK argument of knowledge for the issuance
relation in Section~\ref{sec:issue-relation}.  Its setup produces a CRS; the
security proof may use simulation and extraction trapdoors.  In the real
protocol no trapdoor is available to any party.

The proof may be several megabytes because it is transmitted only during
infrequent issuance.  This design choice is intentional.  Knowledge
soundness alone is enough for decoder safety in a simple stand-alone execution;
simulation extractability is used to support concurrent sessions and the
gated-CCA hybrid proof.

\subsection{Compact threshold trace encryption}

The threshold encryption interface is
\begin{align*}
  (\mathsf{tpk},\mathsf{tsk}_1,\ldots,\mathsf{tsk}_n)
    &\leftarrow \TEnc.\mathsf{KeyGen}(1^\lambda,n,\tau),\\
  C&\leftarrow \TEnc.\mathsf{Enc}(\mathsf{tpk},\mu;\mathsf{ad},\rho),\\
  d_i&\leftarrow \TPDec(\mathsf{tsk}_i,C),\\
  \mu\text{ or }\bot&\leftarrow \TComb(C,\{d_i\}_{i\in I}),\qquad |I|\geq\tau.
\end{align*}

There is an NP relation
\[
 R_{\mathsf{enc}}(\mathsf{tpk},C,\mathsf{ad};\mu,\rho)=1
 \quad\Longleftrightarrow\quad
 C=\TEnc.\mathsf{Enc}(\mathsf{tpk},\mu;\mathsf{ad},\rho).
\]
Only ciphertexts in the language $L_{\mathsf{enc}}$ are promised to be safe
inputs to the distributed decoder.

We require the following post-quantum properties.

\begin{enumerate}[label=(T\arabic*),leftmargin=*]
  \item \textbf{Correctness on valid inputs.}  Combining any $\tau$ correct
  shares for a ciphertext in $L_{\mathsf{enc}}$ returns its plaintext.
  \item \textbf{$\tau$-privacy under chosen plaintext.}  A $\QPT$ adversary
  holding fewer than $\tau$ secret shares cannot distinguish encryptions of two
  equal-length plaintexts.
  \item \textbf{Unique opening.}  It is infeasible to find
  $C,\mu,\rho,\mu',\rho'$ with $\mu\neq\mu'$ such that both
  $R_{\mathsf{enc}}(\mathsf{tpk},C,\mathsf{ad};\mu,\rho)=1$ and
  $R_{\mathsf{enc}}(\mathsf{tpk},C,\mathsf{ad};\mu',\rho')=1$.
  \item \textbf{Robust shares.}  Invalid or inconsistent partial decryptions are
  detected by $\TComb$ or by a component proof/MAC before a plaintext is
  accepted.
\end{enumerate}

Ordinary IND-CCA security of the bare encryption is not required and is not
claimed.

\subsection{Opening authorization}

An independent post-quantum authorization mechanism verifies
\[
  \mathsf{GateAuthVfy}(\mathsf{gavk},d_M,\mathsf{caseID},
  H_{\mathrm{evidence}}(E),\mathsf{auth})=1,
\]
where $d_M=H_{\mathrm{ticket}}(\mathsf{Encode}(M))$.  The authorization binds a
ticket digest, case identifier, evidence digest, expiry, and purpose.  Its
unforgeability and policy governance are orthogonal to trace-ciphertext privacy,
but the binding is required to prevent using an authorization for a different
ticket.

\section{Ticket format and issuance relation}\label{sec:issue-relation}

The hidden ticket payload is
\[
 M=(\ctx,\sn,h,C),
 \qquad h=H_{\mathrm{hold}}(k_{\mathrm{hold}}),
 \qquad C=\mathsf{Ntr.Enc}(\mathsf{tpk},\ctx,h;\rid,\sn,e).
\]
For the v1.3 profile all fields are fixed width and
\[
 \mathsf{Encode}(M)=\ctx\|\sn\|h\|s\|c_\mu\|\gamma
 \in\{0,1\}^{2944},
\]
namely $32+16+32+208+48+32=368$ bytes.  No variable-length field or
user-selected policy string occurs in the payload.
The serial number $\sn\leftarrow\{0,1\}^{128}$ and holder key
$k_{\mathrm{hold}}\leftarrow\{0,1\}^{256}$ are fresh.  The serial appears both
outside and inside the trace ciphertext so that an opener can detect payload
splicing.  The holder secret itself never appears in the ticket.  Define
\[
  m=H_{\mathrm{ticket}}(\mathsf{Encode}(M)).
\]

The public issuance statement and private witness are
\begin{align*}
x&=(\pp,\ctx,\mathsf{sid},\rid,\beta),\\
w&=(M,r,\rho,k_{\mathrm{hold}},e).
\end{align*}
The relation $R_{\mathsf{issue}}(x;w)=1$ iff all of the following hold:
\begin{align}
 \mathsf{TicketShape}(M,\ctx)&=1, \tag{I1}\label{eq:i1}\\
 m&=H_{\mathrm{ticket}}(\mathsf{Encode}(M)), \tag{I2}\label{eq:i2}\\
 R^{\BUOV\text{-}III}_{\mathsf{req}}(\beta;m,r,\rho)&=1,
   \tag{I3}\label{eq:i3}\\
 M.h&=H_{\mathrm{hold}}(k_{\mathrm{hold}}), \tag{I4}\label{eq:i4}\\
 R_{\mathsf{enc}}(\mathsf{tpk},M.C,
   \mathsf{Encode}(\ctx,M.\sn,M.h);\rid\|M.\sn,e)&=1.
   \tag{I5}\label{eq:i5}
\end{align}
The real identity and session identifier are public to the issuer during
issuance but are not part of $M$.  Equation~\eqref{eq:i5} binds the hidden
trace plaintext to the identity authenticated in that exact session.

\section{Construction}

\subsection{Setup}

$\Core.\mathsf{Setup}(1^\lambda,n,\tau)$ performs:
\begin{enumerate}[leftmargin=*]
  \item generate $\Piiss$ parameters and the domain-separated hash parameters;
  \item generate issuer keys
  $(\mathsf{bsk},\mathsf{bvk})\leftarrow\BUOV.\mathsf{KeyGen}(1^\lambda)$;
  \item generate independent opening keys
  $(\mathsf{tpk},\mathsf{tsk}_1,\ldots,\mathsf{tsk}_n)
  \leftarrow\TEnc.\mathsf{KeyGen}(1^\lambda,n,\tau)$;
  \item publish $(\mathsf{ci},\ctx)$ authenticated by a federation
  configuration signature.
\end{enumerate}

No party is allowed to derive $\mathsf{bsk}$ and any $\mathsf{tsk}_i$ from the
same seed or key hierarchy.

\subsection{Blind relation-bound issuance}

For authenticated identity $\rid$ and fresh issuer-side $\mathsf{sid}$:

\begin{enumerate}[leftmargin=*]
  \item The holder samples $\sn,k_{\mathrm{hold}},e$ and computes
  \[
   h=H_{\mathrm{hold}}(k_{\mathrm{hold}}),\qquad
   C=\mathsf{Ntr.Enc}(\mathsf{tpk},\ctx,h;\rid,\sn,e)
  \]
  according to Equations~\eqref{eq:n1}--\eqref{eq:n7},
  constructs $M$, and
  computes $m=H_{\mathrm{ticket}}(\mathsf{Encode}(M))$.
  \item It samples $r\in\{0,1\}^{576}$ and $\rho$, computes
  \[
    c_r=\mathsf{CAP.Commit}(r;\rho),\qquad
    y=r+H_{\RBBC}(m,c_r),
  \]
  sets the public request $\beta=y$, and produces
  \[
     \pi_{\mathsf{issue}}
     \leftarrow \Piiss.\mathsf{Prove}(\mathsf{crs},x,w).
  \]
  \item The issuer checks authenticated identity, epoch, quota, replay status,
  and
  \[
    \Piiss.\mathsf{Vfy}(\mathsf{crs},x,\pi_{\mathsf{issue}})=1.
  \]
  Only then does it execute
  $z\leftarrow\BUOV.\mathsf{Respond}(\mathsf{bsk},y)$.
  \item The holder computes
  \[
    \sigma\leftarrow
    \BUOV.\mathsf{Finalize}(m,c_r,y,z;r,\rho)
  \]
  and accepts only if
  $\BUOV.\mathsf{Vfy}(\mathsf{bvk},m,\sigma)=1$.
\end{enumerate}

The proof $\pi_{\mathsf{issue}}$ is not an extra proof placed beside the
fork's well-built-request proof.  It is the strengthened replacement for
that proof: the original request equations are retained and the ticket,
identity, holder, and trace-ciphertext conjuncts are appended to the proved
relation.  Thus issuance sends one augmented relation proof.

The spendable ticket is $T=(M,\sigma)$.  Neither $\beta$,
$\pi_{\mathsf{issue}}$, $\rid$, nor $\mathsf{sid}$ is transmitted with $T$.

\subsection{Ticket verification}

$\Core.\mathsf{VerifyTicket}(T)$ recomputes
$m=H_{\mathrm{ticket}}(\mathsf{Encode}(M))$, checks canonical payload
encoding and the public common-information policy, then returns
$\BUOV.\mathsf{Vfy}(\mathsf{bvk},m,\sigma)$.

This check proves neither public ciphertext validity nor identity correctness by
itself.  Those facts follow from the certified-issuance theorem and the trust
assumption that the issuer signs only after accepting $\Piiss$.

\subsection{Signature-gated threshold opening}

For an opening request
\[
  Q=(T,E,\mathsf{caseID},\mathsf{auth}),
\]
each honest opening authority runs the following gate before touching its
decoder:

\begin{enumerate}[leftmargin=*]
  \item require $\Core.\mathsf{VerifyTicket}(T)=1$;
  \item require the common epoch and key identifiers to be acceptable;
  \item compute $d_M=H_{\mathrm{ticket}}(\mathsf{Encode}(M))$ and require a
  valid, unused, purpose-limited authorization bound to
  $(d_M,\mathsf{caseID},H_{\mathrm{evidence}}(E))$;
  \item only after all checks pass, output
  $d_i=\TPDec(\mathsf{tsk}_i,M.C)$, bound to the same request digest.
\end{enumerate}

The combiner accepts a reconstructed plaintext $\rid'\|\sn'$ only if robust
share verification succeeds and $\sn'=M.\sn$.  Otherwise it outputs $\bot$.

\paragraph{Critical API rule.}
The implementation MUST NOT export
$\TPDec(\mathsf{tsk}_i,C)$ as a callable production endpoint.  The only exposed
operation is $\Core.\mathsf{OpenShare}(\mathsf{tsk}_i,Q)$, which internally
performs the gate.  This is a security boundary, not an application convention.

\section{Security games}

\subsection{Certified-language soundness}

The challenger maintains a log $\mathcal L$ of accepted issuance sessions.  In
the analysis it uses the extractor to associate each completed session with
$(\rid,M,w)$.  An adversary wins $\mathsf{CertSound}$ if it outputs a ticket
$T=(M,\sigma)$ for which $\Core.\mathsf{VerifyTicket}(T)=1$ and either:
\begin{enumerate}[label=(\alph*),leftmargin=*]
  \item $M$ is not associated with any completed accepted issuance in
  $\mathcal L$; or
  \item every associated extraction fails one of
  Equations~\eqref{eq:i1}--\eqref{eq:i5}.
\end{enumerate}

Distinct messages are counted by their canonical ticket digest.  This game
captures both a new signature and a proof/request substitution.

\subsection{Issuer unlinkability}

An honest-protocol curious issuer chooses or observes two authenticated
identities $\rid_0,\rid_1$ that receive the same $\mathsf{ci}$.  Two honest
holders execute issuance concurrently.  The issuer obtains its full protocol
views, after which it receives the two resulting tickets in random order and
guesses the order.  It may retain all issuance state but receives no opening
shares.  The advantage over $1/2$ is issuer-unlinkability advantage.

This game allows the issuer to know which identity participated in each
issuance; it hides which later ticket came from which session.  It cannot hide
timing, a uniquely sized policy, or a per-user expiration bucket, which is why
common metadata is a stated system requirement.

\subsection{Gated chosen-ciphertext trace privacy}\label{sec:gcca-game}

The game $\mathsf{t\text{-}GCCA}$ is parameterized by threshold $\tau$.
The adversary receives all public parameters and fewer than $\tau$ opening
secret shares.  It may run malicious-user issuance sessions for identities it
is authorized to use, receiving completed tickets, and may submit opening
queries for any validly authorized ticket other than the exact challenge ticket
digest.

For the challenge it selects equal-length identities $\rid_0,\rid_1$ and common
ticket metadata.  The challenger samples $b\leftarrow\{0,1\}$, forms a valid
ticket whose trace ciphertext encrypts $\rid_b\|\sn^*$, and returns
$T^*=(M^*,\sigma^*)$.  The adversary wins by guessing $b$.

The HNCC issuer is not corrupted in this game because it necessarily observes
the authenticated identity at issuance.  HNCC privacy is instead covered by
issuer unlinkability.  The game models an outside observer, an evidence
processor, and fewer than $\tau$ opening authorities.

\begin{remark}[Why the game is not ordinary CCA]
The opening oracle accepts only issuer-certified ticket payloads, not arbitrary
ciphertexts.  Calling the result IND-CCA for bare $\TEnc$ would be incorrect.
The security statement is protocol-specific: certification plus the gate turns
chosen-ciphertext access into chosen-\emph{certified}-ciphertext access.
\end{remark}

\subsection{Trace soundness and non-frameability}

An adversary wins $\mathsf{TraceSound}$ if a valid ticket produced from a
completed accepted issuance authenticated as $\rid$ opens to
$\rid'\neq\rid$, or if it opens with an external serial number different from
the encrypted serial.  The issuer is honest in following the protocol.  The
holder may be malicious.

\section{Security results}

\subsection{Correctness}

\begin{theorem}[End-to-end correctness]\label{thm:correct}
For an honest issuance authenticated as $\rid$, an honestly produced ticket
$T=(M,\sigma)$ verifies.  Any authorized set of at least $\tau$ honest opening
authorities reconstructs $\rid\|M.\sn$, except with the sum of the component
correctness errors.
\end{theorem}

\begin{proof}
Honest computation satisfies every conjunct of $R_{\mathsf{issue}}$, so
$\Piiss$ accepts.  Blind-signature correctness gives a verifying signature
on $H_{\mathrm{ticket}}(\mathsf{Encode}(M))$.
Equation~\eqref{eq:i5} gives a
valid encryption of $\rid\|M.\sn$.  Threshold-encryption correctness on valid
inputs reconstructs that plaintext from any $\tau$ correct shares.  The serial
cross-check therefore succeeds.
\end{proof}

\subsection{Blind-signing message binding}

\begin{lemma}[Request uniqueness]\label{lem:buov-unique}
Let $\beta=y$.  If the PQ-RBBC-BUOV-336 request relation accepts valid openings
for both $m$ and $m'\neq m$, then those values constitute a winning output in
the cross-message game of Theorem~\ref{thm:j-qrom}.
\end{lemma}

\begin{proof}
Expanding the two accepted relations gives distinct inputs $(m,c_r)$ and
$(m',c'_r)$ whose shifted-oracle values are both $y$.  This is precisely the
cross-message collision covered by Theorem~\ref{thm:j-qrom}, including the CAP
extraction and unique-witness bad events.
\end{proof}

\begin{lemma}[Request/proof binding]\label{lem:reqbind}
Suppose an issuance proof for $\beta$ is accepting and extractable.  Except
with cross-message request-collision probability, the message signed through
$\beta$ is exactly $H_{\mathrm{ticket}}(\mathsf{Encode}(M))$ for the payload
$M$ in the extracted witness.  A different payload cannot verify under the same
signed digest except by finding a collision in $H_{\mathrm{ticket}}$.
\end{lemma}

\begin{proof}
Extraction yields $(M,r,\rho,\ldots)$ satisfying
Equations~\eqref{eq:i2}--\eqref{eq:i3}.  Equation~\eqref{eq:i2} fixes $m$ to the
canonical ticket digest.  Lemma~\ref{lem:buov-unique} makes the message
represented by $\beta$ equal to this $m$.  If a different canonical payload
$M'\neq M$ verifies under that signed value, then
$H_{\mathrm{ticket}}(\mathsf{Encode}(M'))=
H_{\mathrm{ticket}}(\mathsf{Encode}(M))$, a collision.
\end{proof}

\begin{theorem}[Certified-language soundness]\label{thm:certsound}
For any $\QPT$ adversary $\mathcal A$, there exist $\QPT$ reductions
$\mathcal B_1,\ldots,\mathcal B_4$ such that
\begin{align*}
 \Adv^{\mathsf{CertSound}}_{\Core,\mathcal A}
 \leq{}&
 \Adv^{\mathsf{SE\text{-}AoK}}_{\Piiss,\mathcal B_1}
 +\Adv^{\mathsf{1mUF}}_{\BUOV,\mathcal B_2}
 +\Adv^{\mathsf{xmsg}}_{J,\mathcal B_3}
 +\Adv^{\mathsf{coll}}_{H_{\mathrm{ticket}},\mathcal B_4}.
\end{align*}
By Theorem~\ref{thm:j-qrom}, the third term is at most the two CAP bad-event
advantages plus $O(q^3/2^{576})$.
Consequently, every verifying ticket is associated with an accepted issuance
witness satisfying $R_{\mathsf{issue}}$, except with negligible probability.
\end{theorem}

\begin{proof}
Consider a verifying output $(M,\sigma)$.  If its canonical digest was not the
message of a completed blind-signing session, then the output is a one-more
forgery.  Otherwise consider the corresponding accepting issuance proof.
Simulation extraction returns a witness or violates the SE-AoK property.
Lemma~\ref{lem:reqbind} associates the signed digest with the extracted payload,
except under the remaining claw and collision bad events.  The extracted witness
then satisfies every issuance conjunct, including ciphertext validity and
identity binding.
\end{proof}

\begin{corollary}[Certified ciphertext validity]\label{cor:validct}
If $\Core.\mathsf{VerifyTicket}(M,\sigma)=1$, then $M.C\in
L_{\mathsf{enc}}$ and it has an opening to the authenticated issuance identity
and the external serial number, except with the bound in
Theorem~\ref{thm:certsound}.
\end{corollary}

\subsection{Decoder safety}

\begin{theorem}[Signature-gated decoder safety]\label{thm:safety}
Assume honest opening authorities expose only
$\Core.\mathsf{OpenShare}$ and execute its checks in the specified order.  The
probability that an honest authority evaluates $\TPDec$ on a ciphertext outside
$L_{\mathsf{enc}}$ is at most
\[
 \Adv^{\mathsf{CertSound}}_{\Core}+\Adv^{\mathsf{forge}}_{\mathsf{GateAuth}}
 +\Adv^{\mathsf{coll}}_{H_{\mathrm{ticket}}}.
\]
\end{theorem}

\begin{proof}
Reaching $\TPDec$ implies that the ticket signature and the opening
authorization both verified for the same ticket digest.  By
Corollary~\ref{cor:validct}, a verifying ticket contains a ciphertext in
$L_{\mathsf{enc}}$.  A mismatch between the ticket checked by the gate and the
ciphertext passed to the decoder either violates the atomic API invariant or
requires a digest collision.  A request not authorized for that digest is an
authorization forgery.  Taking a union bound gives the result.
\end{proof}

\begin{remark}
This theorem explains what is gained by moving the validity proof out of the
online ticket.  The large issuance proof is amortized, while the small issuer
signature becomes a certificate that the ciphertext was once proved valid.
The theorem fails immediately if an implementation offers a raw partial-
decryption endpoint.
\end{remark}

\subsection{Issuer unlinkability}

\begin{theorem}[Issuer unlinkability]\label{thm:unlink}
For an honest-protocol curious issuer, common visible metadata, and two honest
holders, there exist $\QPT$ reductions such that
\begin{align*}
 \Adv^{\mathsf{unlink}}_{\Core}
 \leq{}& \Adv^{\mathsf{blind}}_{\BUOV}
 +2\Adv^{\mathsf{zk}}_{\Piiss}
 +\Adv^{\mathsf{t\text{-}IND\text{-}CPA}}_{\TEnc}
 +\Adv^{\mathsf{coll}}_{H_{\mathrm{ticket}}}.
\end{align*}
\end{theorem}

\begin{proof}
First replace each real issuance proof with a simulated proof; zero knowledge
bounds the change.  The remaining request/response views are ordinary fork
views, so the assumed signer-blindness property permits swapping the two hidden messages
without the issuer detecting the swap.  Threshold-encryption privacy prevents the issuer, which
holds no opening threshold, from matching a final trace ciphertext to either
known issuance identity.  Canonical digest collisions are excluded.  After
these replacements, the issuer's view is independent of the final permutation,
so its success
probability is $1/2$.
\end{proof}

\paragraph{Operational qualifier.}
The theorem is cryptographic, not traffic-analytic.  Issuance batching, fixed
message sizes, delayed activation, and common metadata are required to keep
timing and formatting from becoming a link tag.

\subsection{Trace correctness and non-frameability}

\begin{theorem}[Trace soundness]\label{thm:trace}
Under certified-language soundness and unique opening of $\TEnc$, a malicious
holder completing issuance while authenticated as $\rid$ cannot produce a
verifying ticket that opens to $\rid'\neq\rid$ or to a different serial number,
except with negligible probability.
\end{theorem}

\begin{proof}
By Theorem~\ref{thm:certsound}, a verifying ticket is associated with an
extracted accepted issuance witness.  Equation~\eqref{eq:i5} states that its
ciphertext opens to $\rid\|M.\sn$.  If threshold opening returns a different
plaintext while all robust-share checks pass, either threshold correctness
fails or the ciphertext has two different valid openings.  The latter violates
unique opening.  The explicit serial comparison catches any external/internal
serial mismatch.
\end{proof}

\begin{remark}[Issuer framing]
The theorem assumes an honest-protocol issuer because the issuer learns $\rid$
and controls the signing key.  A malicious issuer could directly sign a payload
encrypting an arbitrary victim identity.  Removing that trust requires a
threshold blind issuer, an auditable issuance log with privacy-preserving
accountability, or retaining a publicly checked proof.  It is not solved by the
present compact-ticket core.
\end{remark}

\subsection{Gated chosen-ciphertext trace privacy}

\begin{theorem}[$\tau$-GCCA trace privacy]\label{thm:gcca}
Assume fewer than $\tau$ opening authorities are corrupted.  For every $\QPT$
adversary $\mathcal A$ in the game of Section~\ref{sec:gcca-game}, there exist $\QPT$
reductions $\mathcal B_i$ such that
\begin{align*}
 \Adv^{\mathsf{t\text{-}GCCA}}_{\Core,\mathcal A}
 \leq{}&
 \Adv^{\mathsf{t\text{-}IND\text{-}CPA}}_{\TEnc,\mathcal B_0}
 +\Adv^{\mathsf{SE\text{-}AoK}}_{\Piiss,\mathcal B_1}
 +\Adv^{\mathsf{1mUF}}_{\BUOV,\mathcal B_2}\\
 &+\Adv^{\mathsf{unique}}_{\TEnc,\mathcal B_3}
 +\Adv^{\mathsf{xmsg}}_{J,\mathcal B_4}\\
 &
 +\Adv^{\mathsf{coll}}_{H_{\mathrm{ticket}},\mathcal B_5}.
\end{align*}
\end{theorem}

\begin{proof}
We use a sequence of games.

\paragraph{Game 0.}
This is the real $\mathsf{t\text{-}GCCA}$ game.

\paragraph{Game 1: extract accepted issuance sessions.}
For every accepted adversarial issuance proof, use the SE-AoK extractor to
record
\[
  (d_M,M,\rid,e,k_{\mathrm{hold}},r,\rho,\ldots)
\]
in a private table $\mathcal L$.  Abort if an accepting proof has no valid
witness.  The difference from Game 0 is bounded by SE-AoK security.  Request
binding and hash collision resistance ensure that the final signed digest is
the digest recorded in $\mathcal L$.

\paragraph{Game 2: reject unlogged verifying tickets.}
If the adversary submits a verifying ticket whose digest is neither the
challenge digest nor in $\mathcal L$, abort.  Such a ticket supplies an
additional valid message/signature pair not obtained from a completed issuance,
which yields a one-more forgery.  Proof/request substitution and digest
ambiguity are charged to the corresponding terms in the theorem.

\paragraph{Game 3: simulate the opening oracle from the issuance table.}
For every non-challenge verifying ticket, look up the extracted plaintext in
$\mathcal L$ and return it after the authorization checks.  This has the same
result as threshold opening by correctness and unique opening, but it uses no
honest opening secret share and never invokes a decryption oracle.  A ticket
that reuses the challenge ciphertext under a newly certified digest requires an
accepted proof containing a valid opening of that ciphertext.  Extraction then
reveals the challenge plaintext.  If the extracted opening disagrees with the
challenge plaintext, it violates unique opening; if it agrees, the reduction
can immediately guess the threshold-encryption challenge bit.  Thus challenge
re-certification does not create an unanswerable decryption query.

\paragraph{Game 4: replace the challenge encryption.}
Embed the $\tau$-IND-CPA challenge of $\TEnc$ as $M^*.C$.  The reduction is
given fewer than $\tau$ shares exactly as in the base game.  It samples the
remaining visible ticket fields identically.  Because it generated the issuer
key and knows the complete challenge message $m^*$, it locally executes the
assumed fork protocol and obtains one exactly distributed final signature;
it does not need the unknown trace-encryption randomness.
The challenge issuance transcript is not given to the adversary in this game,
and $\pi_{\mathsf{issue}}$ influences only the accept/reject gate, not the
distribution of the response after acceptance.  Therefore the reduction may
internally bypass that gate when manufacturing the challenge ticket without
changing any value visible to the adversary.
All opening queries are answered from $\mathcal L$ as in Game 3.  Therefore
changing the encrypted
identity from $\rid_0$ to $\rid_1$ changes the adversary's view by at most the
$\tau$-IND-CPA advantage of $\TEnc$.

In the final game the challenge bit is independent of the adversary's view.
Summing the hybrid differences proves the theorem.
\end{proof}

\paragraph{Why the reduction is useful.}
The large issuance proof is not merely a validity check.  Its extractor gives
the simulator the plaintext and randomness of every non-challenge ciphertext
that can ever obtain a valid issuer signature.  Therefore the simulator can
answer gated opening queries without a base decryption oracle.  This is the
precise reason a CPA-secure compact threshold encryption can suffice inside the
gated protocol even though it would not suffice under an unrestricted CCA API.

\subsection{One-more ticket unforgeability}

\begin{theorem}[Ticket one-more unforgeability]
After $q$ completed accepted blind-issuance sessions, producing more than $q$
distinct verifying ticket digests has probability at most the
one-more-unforgeability advantage of the selected fork, plus the
cross-message request-collision and
ticket-hash collision terms.
\end{theorem}

\begin{proof}
Map every completed issuance to the unique signed digest using
Lemma~\ref{lem:reqbind}.  More distinct verifying digests than completed
sessions then give more valid message/signature pairs than signing responses,
unless two payloads share a digest or Lemma~\ref{lem:buov-unique} fails.
\end{proof}

\section{Candidate concrete instantiations}

\subsection{Augmented well-built-request compiler for the BUOV-336 fork}\label{sec:augcap}

The Blind-UOV framework already requires the user to prove that its masked
request is well built.  We strengthen that proved language instead of adding a
separate policy proof.  Let
\[
  R_{\mathsf{wb}}(y;m,r,\rho)=1
\]
denote Equations~\eqref{eq:b1}--\eqref{eq:b2}.  The native
well-built-request language is replaced by one augmented language:
\begin{align*}
 R_{\mathsf{aug}}(&\pp,\ctx,\mathsf{sid},\rid,y;\\
                  &M,r,\rho,k_{\mathrm{hold}},e)=1
 \quad\Longleftrightarrow\quad
 R_{\mathsf{issue}}(x;w)=1,
\end{align*}
where $\beta=y$ and Equation~\eqref{eq:i3} is $R_{\mathsf{wb}}$.  The issuer
executes the forked signer response only
after verifying a post-quantum SE-NIZK argument for $R_{\mathsf{aug}}$.

\paragraph{Compiler interface.}
The integration replaces the native verification hook:
\[
 \mathsf{VerifyWellBuilt}(y,\pi_1)
 \quad\longrightarrow\quad
 \Piiss.\mathsf{Vfy}(\pp,\ctx,\mathsf{sid},\rid,y,
                    \pi_{\mathsf{issue}}).
\]
The response and finalization shapes are intended to follow the source
framework.  The spendable object is a PQ-RBBC-BUOV-336 fork signature; calling
it a genuine published Blind-UOV-III signature is forbidden until
interoperability is restored, which is not the selected version-2.0 goal.

\paragraph{Concrete circuit boundary.}
The implementation SHOULD expose the following fixed interface to the proof
compiler:
\begin{align*}
 \mathsf{pub}_{\mathsf{aug}}
   &= (\ctx,\mathsf{sid},\rid,\mathsf{tpk},\mathsf{bvk},y),\\
 \mathsf{wit}_{\mathsf{aug}}
   &= (\sn,k_{\mathrm{hold}},e,r,\rho).
\end{align*}
The circuit derives $C$, $h$, $M$, and $m$ internally rather than accepting
duplicate witness copies.  Its constraint system is the conjunction
\[
 \mathcal C_{\mathsf{aug}}=
 \mathcal C_{\mathsf{shape}}\wedge
 \mathcal C_{\mathsf{ticketHash}}\wedge
 \mathcal C_{\mathsf{RBBCmask}}\wedge
 \mathcal C_{\mathsf{holder}}\wedge
 \mathcal C_{\mathsf{trace}},
\]
where
\begin{align*}
 \mathcal C_{\mathsf{shape}} &: \mathsf{TicketShape}(M,\ctx)=1,\\
 \mathcal C_{\mathsf{ticketHash}} &: m=H_{\mathrm{ticket}}(\mathsf{Encode}(M)),\\
 \mathcal C_{\mathsf{RBBCmask}} &: c_r=\mathsf{CAP.Commit}(r;\rho)\ \wedge\
   y=r+H_{\RBBC}(m,c_r),\\
 \mathcal C_{\mathsf{holder}} &: h=H_{\mathrm{hold}}(k_{\mathrm{hold}}),\\
 \mathcal C_{\mathsf{trace}} &: R_{\mathsf{Ntr}}
   (\mathsf{tpk},C,\ctx,h;\rid,\sn,e)=1.
\end{align*}
The proof statement includes $\mathsf{sid}$ even though it is absent from the
ticket.  Consequently, replaying the same proof in a new issuance session
changes the statement and fails verification, while the final credential gains
no session link tag.

\begin{theorem}[Conditional security inheritance of the augmented issuance proof]\label{thm:augcap}
Assume the complete PQ-RBBC-BUOV-336 fork, with its selected CAP and
$H_{\RBBC}$ profile, is post-quantum one-more unforgeable and
honest-protocol signer blind for its native well-built-request language.
Assume also that $\Piiss$ is post-quantum zero knowledge and
simulation-extractable for $R_{\mathsf{aug}}$.  Replacing the fork's native
well-built-request proof by $\pi_{\mathsf{issue}}$ preserves those two assumed
properties, up to the SE-NIZK, cross-message request, and hash-collision terms
already shown in Theorems~\ref{thm:certsound} and~\ref{thm:unlink}.
\end{theorem}

\begin{proof}
For one-more unforgeability, a forger against the augmented scheme is also a
forger against the completed fork with its native well-built proof: the
reduction verifies the augmented proof and forwards each accepted $y$ to the
fork signer instance.  If that interface requires a native well-built proof,
the reduction extracts $(m,r,\rho)$ and generates it.  Extraction failure is
charged to SE-AoK.  The augmented relation restricts the fork's native
well-built language.

For blindness, replace the augmented proof by a simulated proof.  The remaining
transcripts are ordinary fork transcripts, so the assumed fork blindness game
applies directly.  The
public authenticated identity is session information but is absent from the
final ticket; threshold-encryption privacy hides it inside $M.C$.  Any attempt
to make the same masked request represent a different ticket digest triggers
Lemma~\ref{lem:buov-unique}, and any two payloads with one ticket digest trigger
a collision in $H_{\mathrm{ticket}}$.
\end{proof}

This theorem is no longer a reduction to the published Blind-UOV parameter
set.  Its first premise is a version-2.0 proof obligation.  The executable
sponge and row tests establish functional consistency only; they do not
establish one-more unforgeability or blindness of the complete fork.

\paragraph{Corrected concretization in version 1.7.}
The request and witness are now fixed:
\begin{align*}
 \beta&=y,& b&=(r,\rho),\\
 m&=H_{\mathrm{ticket}}(\mathsf{Encode}(M)),&
 c_r&=\mathsf{CAP.Commit}(r;\rho),\\
 y&=r+H_{\RBBC}(m,c_r).&&
\end{align*}
The signer sees only the 72-byte value $y$, not $c_r$ or $m$.
Earlier drafts incorrectly exposed $c_r$ and claimed that CAP binding plus hash
collision resistance implied request uniqueness.  That argument fails when an
attacker chooses different commitments on both sides.  Version 1.6 restored the
paper ABI but then incorrectly multiplied the security of two independently
openable NIST-I lanes.  Version 1.7 instead uses a single 576-bit
Blind-UOV-III target and Theorem~\ref{thm:j-qrom}.  The removed
ticket commitment $c_M$ remains unnecessary: extraction supplies $M$, while
ticket-hash collision resistance prevents a second payload under one digest.

\paragraph{Executable refinement through version 2.4.}
Introduce the hidden 576-bit value
\[
 h_{\RBBC}=H_{\RBBC}(m,c_r).
\]
The executable incremental circuit allocates $r$ and $h_{\RBBC}$ as secret
binary inputs, enforces their bitness, and materializes all 576 linear equations
$y_j+r_j+h_{\RBBC,j}=0$.  Its only remaining external assertion is the narrower
native subrelation
\[
 h_{\RBBC}=H_{\RBBC}(m,\mathsf{CAP.Commit}(r;\rho)),
\]
where $m$ is passed directly from the circuit-produced ticket-digest wires.
Thus a future native importer cannot choose a different $y$, $r$, hash image,
or ticket message without failing an ordinary circuit row.
Version 2.0 replaced the test hash-image computation with the forked Anemoi
sponge while retaining a test-only CAP commitment.  Version 2.1 adds a strict
ABI that accepts only the canonical 5,391-byte production-profile CAP encoding
and its frozen profile fingerprint before computing $H_{\RBBC}$.  This closes
the byte-level join; the full native row identities across the 18 CAP trees and
the hash calls remain external.
Version 2.2 closes those identities for the reduced fixture.  Version 2.3 also
closes every squeeze-state identity for production-width 2,450-bit leaf tapes
inside a deliberately small extended fixture.  The production parent relation
still keeps one external assertion.  Version 2.4 natively closes the generic
2,048-bit Horner gadget and exercises its symbolic-mask integration in a
two-coefficient CAP fixture; the full 11-coefficient, 18-tree composition is
not yet materialized.  Version 2.5 executes the full eleven-coefficient path
and every degree-12 symbolic mask slice in one real 2,048-leaf production tree
shape, while deliberately stopping short of a full assignment archive and the
18-tree parent composition.  Version 2.6 removes the first of those two
limitations: the complete one-tree assignment is archived and every row is
evaluated successfully, while the 18-tree parent composition remains open.
Version 2.7 applies the same discipline to one real 4,096-leaf, degree-13
tree.  Both production tree shapes are therefore closed separately, but they
have not yet been composed under one native row transcript.  Version 2.8 now
executes them under one reference transcript and freezes their canonical link
schedule; the native global-tail and parent wire identities remain external.
Version 2.9 now materializes and completely replays that shared global tail.
The remaining native gap is narrower: generate all eighteen position-specific
tree-producer segments without duplicated tails, relocate their exact output
wires into the frozen tail ports, and replay the combined composition before
performing the parent join.
Version 2.10 tests that segmentation boundary end to end on both positions of
the reduced profile.  It closes the producer constraints and value-level port
ABI for that non-secure fixture, but deliberately leaves point-wire identity,
the two production shapes, all eighteen production positions, and the parent
join open.

\paragraph{Frozen Blind-UOV comparison profile.}
Version 1.7 fixes the 31 October 2025 revision and its
\emph{Blind-UOV-III, Shorter, TCitH} row.  The paper gives security parameter
$\lambda=192$, $\bar n_r=576$ bits, $\bar n_x=1472$ bits, a 189.2~KB public
key, and an 11,644-byte final signature~\cite{blind-uov}.  Version 2.4 retains
the 576-bit request width, but the key and signature figures are comparison
targets only.  They must be regenerated for the independent fork before any
online-size claim is labelled measured or security-qualified.

\paragraph{Correct backend separation.}
Blind-UOV has two different proof objects.  Its CAP commitment and final CAP
proof are carried by the final blind signature.  Before the signer responds,
the user separately sends a NIZK $\pi_1$ proving that the masked request and
CAP commitment are well formed.  Our $\pi_{\mathsf{issue}}$ replaces and
augments this $\pi_1$; it does \emph{not} replace the final CAP proof.
The augmented proof still replaces only $\pi_1$, not the final CAP proof.
Nevertheless, version 2.4 may not assert that the final signature remains
11,644 bytes: the extra forked permutation work and any changed transcript must
be implemented and benchmarked first.

\paragraph{Native import contract and field boundary.}
For NIST-III/Shorter/TCitH, Table~2 of the paper fixes
$\tau=18$: two $2^{12}$-leaf trees and sixteen $2^{11}$-leaf trees,
$T_{\mathrm{open}}=174$, an explicit proof-of-work of 9 bits, and total
proof-of-work 13.9 bits.  The SNARK-friendly level-III profile uses an
eight-element Anemoi state over $\mathbb F_{2^{193}}$, with 240 R1CS
constraints per permutation~\cite{blind-uov}.  In particular, the abstract
$\rho$ contains the CAP salt and GGM-tree randomness; it is not the 32-byte
nonce used by the test adapter.

The paper specifies the algorithms but does not ship an executable CAP
constraint generator.  Version 2.6 uses
\texttt{pq\_rbbc\_native\_profile.py} as the fail-closed contract for the fork.
Closure requires a source and parameter digest, a deterministic native
row-stream digest, zero external assertions, witness-independent topology,
verified fork vectors, message/mask/hash-image wire identity, and four
independent tamper rejections.  It additionally requires all seven named
CAP/GGM/transcript components, a fork-specific security proof, and fresh
signature-size benchmarks.  Claiming bit-exact Blind-UOV compatibility is an
explicit audit failure.  The complete augmented relation must also be lifted
from the portable $\mathbb F_2$ archive into $\mathbb F_{2^{193}}$.  This
structural certificate prevents accidental under-constraint; it is not a
substitute for cryptographic review.  The official Anemoi repository supplies
permutation and equation-generation code, but not a complete CAP importer
\cite{anemoi-code}.

\paragraph{Source-pinned native Anemoi component in version 1.9.}
The companion implementation
\texttt{pq\_rbbc\_anemoi\_f193.py} uses the polynomial basis defined by
\[
 X^{193}+X^8+X^7+X^6+X^5+X^4+X^2+X+1,
\]
the Conway polynomial $f_{2,193}$ published by Luebeck
\cite{conway-polynomials}.  It pins the official Anemoi repository at commit
\path{3e86ff0cafa54839709b2fa2de0e75d7dd2db464}, together with the SHA-256
digest of its Sage source.  For security level 192,
four columns, and $\alpha=3$, that source derives 14 rounds.  The explicit
characteristic-two estimate in the Anemoi paper instead derives 15 rounds
\cite{anemoi-paper}.

The characteristic-two closed-Flystel equations contain two cubic terms.  The
implemented direct lowering uses six multiplication rows per column per round:
four square/cube rows and two closed equations.  Consequently the pinned
14-round permutation has $14\cdot4\cdot6=336$ nonlinear rows; the paper-rule
15-round variant would have 360.  Blind-UOV reports 240 constraints per
level-III permutation but publishes neither the bit-exact parameter file nor a
constraint stream reproducing that number~\cite{blind-uov}.  These values may
reflect different unpublished parameters or an optimized lowering, but the
public artifacts do not decide which.  Version 1.9 therefore records the
$240/336/360$ gap as unresolved and forbids production closure.

For the frozen zero-state topology, the native component archive contains 336
nonlinear rows, eight final-state binding rows, and 352 allocated wires.  Its
deterministic row-stream digest is
\texttt{25deba5f7fa3f54f1ccc2fd165f2755f8d7137eaa924def12f0c28ba5cdbae4d}.
Regression tests check field reduction and inversion, parameter derivation,
direct-versus-constrained outputs on three inputs, witness-independent row
topology, and stale-witness rejection after input or output mutation.  This is
one permutation component only: it is not a sponge, CAP.Commit, GGM tree,
Fiat--Shamir transcript, or complete Blind-UOV hash relation.  Independent
Sage-generated vectors and all seven CAP components are still required.

\paragraph{Independent sponge and request hash in version 2.0.}
The selected profile is
\emph{PQ-RBBC-Anemoi-193/336-Sponge-v1}.  Its fingerprint is
\begin{center}
\texttt{4fa0eb276ebba70a9f6c2f38f3f55d19}\par\vspace{-0.2em}
\texttt{7c094121a2b614cc6ef9b7e8522cac87}.
\end{center}
It divides the eight-element state into four rate and four capacity elements,
so both rate and capacity are 772 bits.  For domain string $D$ and payload $P$,
the absorbed bit string is the injective encoding
\[
 \mathtt{PQRBBC\mbox{-}SPONGE\mbox{-}V1}
 \|\mathsf{u16le}(|D|)\|D\|\mathsf{u64le}(|P|)\|P\|\mathsf{pad}_{10^*1},
\]
with bytes and polynomial-basis coefficients interpreted least-significant bit
first.  The request hash uses domain
\path{PQ-RBBC/v2.0/H_RBBC}; its payload is an injective ordered-tuple encoding
of $(m,c_r)$, and its output is the first 576 squeezed bits.

For the frozen 32-byte message and 48-byte test-commitment vector, the encoded
payload is 118 bytes and absorption takes two permutations.  The native trace
contains 944 input-bitness rows, 672 permutation multiplication rows, 579
output-bitness rows, and 43 linear packing/chaining rows: 2,238 rows and 2,235
wires in total.  Its deterministic row-stream SHA-256 is
\begin{center}
\texttt{3aa60bc6d8d507003fb541a6ac991e88}\par\vspace{-0.2em}
\texttt{da58aea05b7b278ab7e1859772aac9ed}.
\end{center}
Tests reject payload and output mutation, compare constrained and direct hashes
on four payloads, check domain and length separation, and prove that the row
topology is witness-independent for fixed public lengths.

This closes the permutation-to-hash engineering layer.  The historical
48-byte commitment remains only a test fixture for the incremental circuit; it
is not the production CAP encoding.

\paragraph{Source-grounded CAP.Commit reference in version 2.1.}
The companion module \texttt{pq\_rbbc\_cap\_commit.py} directly instantiates
the initial-commitment logic of Protocols 8--10 of the cited Blind-UOV
revision.  It implements salted GGM seed derivation, leaf-seed commitments,
leaf-tape expansion, degree-one polynomial coefficients, corrections forcing a
common constant term across repetitions, polynomial-universal consistency
hashing, mask derivation, and later signature-append deltas.  Because the paper
does not provide an executable CAP generator, this is an independently named
fork rather than a bit-exact reproduction.  Its relation identifier is
\path{pq-rbbc/cap/tcith-iii/anemoi-193-336/v1} and its profile fingerprint is
\begin{center}
\texttt{2ac471f8d7c6cb4e6352bbc5a2eb7f93}\par\vspace{-0.2em}
\texttt{94b807ff132aec8cadebd696f7b1fa38}.
\end{center}

The production shape commits a 2,048-bit upper-bound witness consisting of a
576-bit mask and 1,472 later signature bits.  It uses two $2^{12}$-leaf trees
and sixteen $2^{11}$-leaf trees.  Protocol~8 needs an injection of all leaves
into nonzero extension-field elements.  For a power-of-two leaf count,
$\mathbb F_{2^k}^{*}$ has only $2^k-1$ elements; this fork therefore uses the
safe degrees $\mu=13$ and $\mu=12$, rather than 12 and 11.  It fixes
$\rho=16$, two $\mathbb F_{2^{193}}$ consistency evaluation points (386 bits),
and a 2,450-bit tape per leaf.  These are fork parameters and require their own
security analysis.

The canonical commitment encoding is exactly 5,391 bytes.  It contains the
profile fingerprint, two salt elements, the 386-bit $h_2$, a 386-bit
consistency value, and 17 pairs of 2,048-bit and 386-bit corrections.  It is
hidden inside $\pi_{\mathsf{issue}}$: the signer still receives exactly the
72-byte masked target $y$, not $c_r$ or $m$.  A strict API rejects reduced
fixtures, wrong profile fingerprints, or any non-canonical length before
joining these bytes to $H_{\RBBC}(m,c_r)$.

Version 2.8 corrects the earlier 5,378-byte accounting value.  The serializer
byte-aligns $\alpha$, every $\Delta P_i$, and every
$\Delta\widehat M_i$ separately, whereas the old helper rounded the combined
bit count only once.  The former helper therefore undercounted production by
13 bytes (and the reduced two-tree vector by one byte).  The actual serializer,
strict ABI, manifests, and tests now agree on 5,391 bytes.

The reduced, explicitly non-secure two-tree fixture freezes profile fingerprint
\begin{center}
\texttt{5d067a55e2ea9104b2604dc7efa393f4}\par\vspace{-0.2em}
\texttt{4d1ce1880c3974bdcaae32aeb825f2ea}
\end{center}
and commitment digest
\begin{center}
\texttt{07a09a4f623233586af7ebca90d0eeba}\par\vspace{-0.2em}
\texttt{7d6a5bb94ff86c7dff29ade7be79b800}.
\end{center}
It executes 23 domain-separated XOF calls.  Regression tests verify its frozen
output, correction identities, mask/signature round trips, domain separation,
the strict $H_{\RBBC}$ join, and rejection after salt, root, message, profile,
or length mutation.

For the full topology, exact framing arithmetic gives 40,924 seed-derivation
calls, 40,960 leaf commitments, 40,960 tape expansions, and three transcript
XOF calls: 122,847 XOF calls in total.  Under the selected sponge this expands
to 389,974 Anemoi permutations and 131,031,264 permutation nonlinear rows.
This count omits non-permutation native rows for extension-field operations,
corrections, packing, and wire equalities.  It is therefore a lower component
count, not a complete $\pi_{\mathsf{issue}}$ benchmark.

Version 2.8 runs the opt-in full 18-tree production reference.  It freezes the
ordered 122,847-call XOF trace, every tree component, all 17 correction pairs,
one 5,391-byte commitment, and the final request hash.  Both constituent tree
shapes already have independent whole-assignment evidence.  No deduplicated
18-tree native row stream, shared global-tail assignment, or parent inter-call
wire-identity proof has yet been produced.  Consequently the parent archive
still has one named external assertion.  Reference execution and the exact
byte ABI do not establish native circuit closure, CAP extraction, fork-specific
blindness, one-more unforgeability, or the paper's signature size.

\paragraph{Zero-callback reduced native lowering in version 2.2.}
The module \path{pq_rbbc_cap_native.py} replays all 23 reduced CAP XOF
calls and one final request-binding call as native Anemoi sponge traces.  It
links each encoded payload bit to its originating salt, root, parent seed, leaf
seed, tape, correction, consistency value, canonical commitment bit, or message
bit with ordinary field rows.  The canonical 215-byte reduced commitment is an
explicit constrained output, and those exact wires feed the 576-bit
$H_{\RBBC}(m,c_r)$ trace.  Verification evaluates only the resulting rank-one
rows; it invokes no CAP or hash callback and records zero external assertions.

The frozen reduced component contains 59,602 wires and 88,282 rows.  Of these,
58,462 are nonlinear under the direct model and 29,820 are linear.  Its 24 XOF
calls require 57 Anemoi permutations, contributing 19,152 permutation rows;
the remainder includes payload/output bitness, framing, packing, and 26,848
source-link rows.  The 51,845,969-byte canonical JSON row stream has SHA-256
\begin{center}
\texttt{f6a6a0b65e6de16f7bb1d6b42302a12}\par\vspace{-0.2em}
\texttt{b004befa62b629d252861e2c986917263}.
\end{center}
Changing salt, a GGM root, and the message changes the assignment and outputs
but leaves this row-stream digest unchanged.  Separate stale-witness tests flip
salt, message, commitment, and request-hash bits; every mutation violates an
ordinary row.

This closure is intentionally narrow.  The reduced witness has only 64 bits,
so its polynomial hash occupies one $\mathbb F_{2^{193}}$ coefficient and the
non-XOF consistency algebra is linear.  Production requires a 2,048-bit
multi-coefficient polynomial hash and a 2,450-bit tape, exceeding one 772-bit
squeeze block.  Version 2.2 therefore rejects production parameters.  The
production parent archive retains its one external assertion.

\paragraph{Production-width multi-squeeze lowering in version 2.3.}
The direct sponge evaluator already defined ordinary sponge continuation, but
the version-2.2 native relation exposed at most one 772-bit rate block.  Version
2.3 adds a native permutation between every later squeeze block and links all
eight input state lanes to the preceding constrained output state.  Each
exposed lane is decomposed into 193 binary wires and packed back to the state
element; a partial final lane exposes only the requested prefix while still
constraining the complete field element.  There is no counter or callback: the
construction follows the frozen sponge's state-continuation semantics exactly.

For the deterministic CAP tape input, 2,450 bits require four squeeze blocks
and three additional permutations.  Together with two absorb permutations,
the standalone trace has 4,845 wires and 4,858 rows: 1,680 permutation rows,
568 payload-bitness rows, 2,509 output-bitness rows, and 101 linear rows.  Its
3,216,517-byte canonical row stream has SHA-256
\begin{center}
\texttt{c42aec869c6de08695bcb11e2fade158}\par\vspace{-0.2em}
\texttt{cc4683aa3ac999e440b78c6ad0314c34}.
\end{center}
Flipping a bit in the second output block violates its packing row; flipping
an intermediate squeeze state violates the next permutation-input link.
Changing the seed while preserving lengths leaves the row stream byte-for-byte
unchanged.

The extended CAP fixture keeps two four-leaf trees and a 64-bit witness but
sets the tape width to the production value.  All eight tape XOF calls therefore
exercise the four-block path.  The resulting component has 85,034 wires and
113,802 rows, of which 83,510 are nonlinear and 30,292 are linear.  Its 24 XOF
calls use 81 Anemoi permutations.  The 69,273,394-byte row stream has SHA-256
\begin{center}
\texttt{98222b0cafeb944184e3939a878d1e3f}\par\vspace{-0.2em}
\texttt{b3af05d10c9795ef0701c87f95462855}.
\end{center}
All XOF payloads, the 215-byte canonical commitment, and the final
$H_{\RBBC}$ join remain ordinary rows with zero external assertions.  This is
not a security parameter set: the witness still occupies one
$\mathbb F_{2^{193}}$ coefficient, so it does not test variable-point field
multiplication or the production 18-tree topology.

\paragraph{Native field multiplication and Horner hashing in version 2.4.}
Write the polynomial-basis packing of LSB-first bits as
\[
 \mathsf{Pack}(b_0,\ldots,b_{\ell-1})=
 \sum_{j=0}^{\ell-1} b_j X^j\in\mathbb F_{2^{193}},
 \qquad \ell\leq 193.
\]
For packed operands $A$ and $B$, the backend row $A\cdot B=C$ is one native
rank-one constraint; reduction modulo the frozen Conway polynomial is the
field operation, not an unconstrained host computation.  Usability as a
bit-facing gadget additionally requires 386 input-bitness rows, 193
output-bitness rows, and one output-pack equality.  The standalone fixture
therefore has 580 wires and 581 rows.  Its product is
\begin{center}
\texttt{011e6dabdb4a6f680c8f194ffd9d0dfc4a7a20a7a5599900c9},
\end{center}
and its 269,342-byte row stream has SHA-256
\begin{center}
\texttt{1c2c2bb9fa869fd43f1bc1f7089e5e0}\par\vspace{-0.2em}
\texttt{5a6f9e5cba1d1c8d0a6a80039417681f7}.
\end{center}

For a bit vector $P$, define coefficients $c_i$ by consecutive 193-bit
chunks.  At point $a$, the native relation evaluates
\[
 z\leftarrow c_{q-1},\qquad
 z\leftarrow z a+c_i\quad(i=q-2,\ldots,0).
\]
The 2,048-bit production width gives $q=11$ coefficients, so two evaluation
points require exactly $2(q-1)=20$ multiplication rows.  The relation also
constrains $a_0,a_1\neq0$ and $a_0\neq a_1$ through inverse witnesses:
\[
 a_0u_0=1,\qquad a_1u_1=1,\qquad(a_0+a_1)v=1.
\]
It then bit-decomposes both outputs.  The frozen standalone trace has 2,843
wires and 2,845 rows (2,843 nonlinear and two linear), and zero external
assertions.  Its 1,714,967-byte row stream has SHA-256
\begin{center}
\texttt{0c9d742d44808a20a35838be84a63892}\par\vspace{-0.2em}
\texttt{4dc5b2f9183bba731eefba1cb9069850}.
\end{center}
Regression tests compare both outputs against the direct CAP polynomial
evaluator and reject stale witnesses after changing the vector, either point,
an intermediate product, or an output bit.  A second valid vector/point pair
produces the identical row stream, establishing topology independence for
fixed widths.

\paragraph{Symbolic-mask CAP integration in version 2.4.}
The combined fixture uses a 386-bit witness (two coefficients), two consistency
points, two four-leaf trees of extension degree three, and
$\rho=1678$, hence $386+1678+386=2450$ tape bits.  The plain vector needs one
Horner call.  Each of the two trees contributes three extension-bit slices,
so the mask algebra needs six more calls.  Across seven calls this gives 14
multiplication rows, three point-validation rows, 2,702 output-bitness rows,
and 14 output-pack rows.

The complete zero-callback component contains 92,816 wires and 125,401 rows:
91,232 nonlinear and 34,169 linear.  Its 24 XOF calls use 84 Anemoi
permutations.  The 77,156,408-byte row stream has SHA-256
\begin{center}
\texttt{ca391f7d64f649b26c98d646dd1c382a}\par\vspace{-0.2em}
\texttt{ebcf848ab849316cb1f65d040c184525},
\end{center}
and the canonical 304-byte commitment has SHA-256
\begin{center}
\texttt{52888b6f229d4e252534d594d0ceaf9b}\par\vspace{-0.2em}
\texttt{991be649a1230e269ea6273762499e21}.
\end{center}
The native commitment and $H_{\RBBC}$ output equal the direct reference.
Separate mutations of a plain Horner product, a symbolic-mask Horner product,
and an output bit violate ordinary rows.  This fixture is intentionally
non-secure: it demonstrates the missing algebraic composition but does not
replace the 18-tree production trace or establish fork-specific extraction.

\paragraph{Bounded-memory production-tree shard in version 2.5.}
The first production-shape shard instantiates one of the sixteen
$2^{11}$-leaf trees.  Its witness width is 2,048 bits, so every leaf invokes
the eleven-coefficient Horner relation at both transcript-derived points.  The
degree-12 extension representation contributes twelve symbolic mask slices.
Linearity permits the implementation to evaluate each leaf once per point and
then aggregate the two field outputs into the plain value and the selected
extension slices.  It therefore avoids constructing 2,450 coordinate-wide
mask vectors in memory.

Only the 2,048 witness wire identifiers and 386 $\widehat M$ wire identifiers
per leaf are retained in a temporary little-endian spool.  The spool is
39,878,656 bytes and has SHA-256
\begin{center}
\texttt{87960a5803e2663a40b3c0bda1611840}\par\vspace{-0.2em}
\texttt{806e649f3927c238bd63bdce08812f49}.
\end{center}
Every other row is serialized directly into a canonical NDJSON SHA-256 sink.
The sink counts 6,146 XOF calls including request binding, 19,505 Anemoi
permutations, 40,960 Horner multiplication rows, 28,546 field-aggregation
rows, three point-validation rows, and 5,018 final output-bitness rows.

The complete streamed topology contains 19,903,324 wires and 26,126,283 rows:
19,509,254 nonlinear and 6,617,029 linear.  The virtual canonical row stream is
18,869,935,441 bytes and has SHA-256
\begin{center}
\texttt{2cfc3641a94635af35dfa5494c61e74a}\par\vspace{-0.2em}
\texttt{416ef2fb446975cd417891d244943dfc}.
\end{center}
The deterministic one-tree commitment is 206 bytes with SHA-256
\begin{center}
\texttt{14fab7548083411124176bb8e094628f}\par\vspace{-0.2em}
\texttt{e6d20347cd78929573b76ab2cd3e757a}.
\end{center}
On the recorded run, eight worker processes completed the reference and row
stream in 781.76 seconds with peak RSS 140,932~KiB.  These are engineering
measurements, not prover benchmarks.

\begin{proposition}[One-shard streamed topology closure]
For the frozen one-tree profile, every GGM, leaf commitment, tape expansion,
Horner multiplication, extension-slice aggregation, transcript hash,
canonical commitment bit, and request-binding bit is represented by an
ordinary native row or a wire equality, and the streamed relation records zero
external assertions.
\end{proposition}
\begin{proof}
The native permutation template is locally satisfiable and is remapped by
disjoint wire offsets for every sponge call.  Each payload wire is bit
constrained and linked to a repeatable source linear form.  Each leaf witness
is split into eleven consecutive field coefficients; Horner evaluation uses
ten native multiplications per point.  Field-level xor aggregation is sound by
linearity over characteristic two, and the final plain and twelve mask values
are decomposed and packed back into constrained bits.  H1, the two validated
points, H2, canonical serialization, and the request hash consume exactly
those wire forms.  Canonical serialization counts and hashes every emitted
row, and the manifest records an external-assertion count of zero.
\end{proof}

The proposition was intentionally about topology, not full shard witness
evaluation or production security.  Version 2.6 supplies the previously
missing assignment evidence as follows.

\paragraph{Assignment-backed shard verification in version 2.6.}
Wire $i$ is stored at offset $128+25(i-1)$ as the canonical little-endian
representation of one $\mathbb F_{2^{193}}$ element.  The 128-byte header binds
the field degree, element width, wire count, assignment-body digest, and
canonical row-stream digest.  The body is 497,583,100 bytes with SHA-256
\begin{center}
\texttt{e16ca6a9228f9f13901d0e0228751010}\par\vspace{-0.2em}
\texttt{fa25889ed02a7291aaceebe69590843a}.
\end{center}
Including the header, the archive is 497,583,228 bytes with SHA-256
\begin{center}
\texttt{6df38b0cadc2390ea953511ed20c1c22}\par\vspace{-0.2em}
\texttt{668f85f63a0519965f2d5a78b44d0095}.
\end{center}

The writer receives allocation-order values from a bounded ordered worker
pool.  Sponge witnesses include payload bits, framed rate lanes, all native
permutation intermediates, and output decompositions.  Horner products,
point inverses, accumulators, public outputs, and request-binding values are
computed in the same allocation order as the unchanged row generator.  No
list of 19.9 million Python integers is retained.

\begin{proposition}[One-shard assignment satisfiability closure]
For the frozen one-tree profile and deterministic vector, the complete
19,903,324-wire assignment satisfies all 26,126,283 canonical native rows.
The replayed row stream has the version-2.5 digest, and changing one archived
wire in each of five selected subsystems violates the corresponding exact row.
\end{proposition}
\begin{proof}
The archive reader first checks its size, fixed profile fields, assignment-body
digest, and embedded row-stream digest.  The row generator is then replayed
without an assignment writer.  For every emitted rank-one row $(L,R,O)$, the
verifier resolves each linear form from the mmap-backed archive and checks
$L(\mathbf w)R(\mathbf w)=O(\mathbf w)$ in $\mathbb F_{2^{193}}$; bitness,
linear, and square rows use algebraically equivalent specialized evaluators.
The replay checks 26,126,283 rows, records zero failures, and regenerates
SHA-256
\begin{center}
\texttt{2cfc3641a94635af35dfa5494c61e74a}\par\vspace{-0.2em}
\texttt{416ef2fb446975cd417891d244943dfc}.
\end{center}
Finally, the test harness flips the low bit of one wire in an exact GGM
payload-source row, tape-output pack row, Horner multiplication row,
commitment-link row, and request-binding pack row.  The honest assignment
satisfies each captured row and every mutated assignment fails it.
\end{proof}

This proposition closes whole-shard satisfiability for one deterministic,
non-secure $2^{11}$-leaf fixture.  It does not prove CAP unique-mask or
straightline extraction, does not instantiate either $2^{12}$-leaf tree, and
does not connect this assignment to the parent issuance archive.  Those
remaining boundaries prevent any production-security claim.

\paragraph{Assignment-backed degree-13 shard verification in version 2.7.}
The second production shape has 4,096 leaves.  Its extension degree must be at
least 13: the multiplicative group of the degree-12 field contains only 4,095
nonzero elements, one fewer than the distinct nonzero evaluation points
required by the leaf-index injection.  The witness still has 2,048 bits in
eleven field coefficients, two consistency points, and 2,450 tape bits per
leaf.

\begin{proposition}[Degree-13 shard assignment satisfiability closure]
For the frozen 4,096-leaf one-tree profile and deterministic vector, the
complete 39,789,564-wire assignment satisfies all 52,224,501 canonical native
rows.  The generation and verification row streams have identical topology,
and one stale wire in each of five selected subsystems is rejected.
\end{proposition}
\begin{proof}
The fixed-width archive uses the same 128-byte header and canonical 25-byte
field elements as version 2.6.  Its body is 994,739,100 bytes with SHA-256
\begin{center}
\texttt{e61fc4fec72b302a0eaf83680044242c}\par\vspace{-0.2em}
\texttt{5cc87aedc79db23fec6d681e55f04947},
\end{center}
and the full 994,739,228-byte archive has SHA-256
\begin{center}
\texttt{e4dea88f7f47849cd858d3ba2d5110bd}\par\vspace{-0.2em}
\texttt{1893efb1ac4544a8b2cb8a0e7fa87aa1}.
\end{center}
The generator emits 38,997,232 nonlinear rows and 13,227,269 linear rows.
Their 37,955,986,032-byte virtual canonical stream has SHA-256
\begin{center}
\texttt{0d921a379af0f8c7bd34bb9c3804cbbb}\par\vspace{-0.2em}
\texttt{32daea51ea827d753a8493758c892530}.
\end{center}
The archive reader validates the header, size, body digest, and embedded row
digest, then resolves every linear form through mmap and checks all 52,224,501
rank-one equations over $\mathbb F_{2^{193}}$.  It records zero failures and
the same row-stream digest.  Finally the low bit of one selected wire is
flipped in a GGM source-link row, leaf tape-pack row, Horner-multiplication
row, commitment-link row, and request-binding pack row.  Every honest row
accepts and every stale assignment fails its selected row.
\end{proof}

The degree-13 run uses 12,290 XOF calls including request binding, 38,999
Anemoi permutations, and a 79,757,312-byte compact wire spool.  Generation
after the concrete reference execution took 1,130.766 seconds; archive
validation and complete row replay took 1,619.663 seconds.  Peak resident set
recorded by the generator was 254,220~KiB.  These are relation-construction
measurements, not prover or proof-verifier benchmarks.  As with the degree-12
fixture, this proposition establishes deterministic assignment
satisfiability, not CAP extraction, zero knowledge, or production security.

\paragraph{Canonical 18-tree reference composition in version 2.8.}
Let the production tree order be
\[
 ((4096,13),(4096,13),\underbrace{(2048,12),\ldots,(2048,12)}_{16}).
\]
The composer derives deterministic full-profile salt and roots, executes all
trees, computes the 17 pairs $(\Delta P_i,\Delta\widehat M_i)$ relative to tree
zero, and derives $H_1$, two nonzero distinct consistency points, $\alpha$,
all 18 $\xi_i$ components, $H_2$, the canonical commitment, and
$H_{\RBBC}(m,c_r)$.  A reduced-profile regression first proves that the
parallel executor is object-for-object equal to the original direct executor,
including every ordered XOF call.

\begin{proposition}[Full-reference composition and linked-schedule closure]
For the frozen production randomness and 32-byte zero request message, the
version-2.8 execution contains 18 trees, 40,960 leaves, 122,847 ordered CAP XOF
calls, and 17 correction pairs.  Its 5,391-byte commitment and request hash are
bound by one canonical linked document.  Changing tree order, constituent
shard evidence, one correction, the commitment digest, or the request-message
digest causes validation to reject.
\end{proposition}
\begin{proof}
The profile itself fixes the tree order and the safe extension degree for each
position.  Every linked record contains its tree index, shape, cumulative
template offsets, exact frozen one-tree row and assignment digests, and the
SHA-256 digest of the position-sensitive tree component.  The correction
component contains 17 length-checked 2,048-bit values and 17 length-checked
386-bit values.  The global record commits to $H_1,H_2,\alpha$, the serialized
commitment, request message, and request hash.  Finally, a length-framed trace
digest covers every call label, domain, injectively encoded payload, output
width, and output in order.  The validator recomputes these values from the
execution.  Each of the five mutations changes either a profile-order
invariant, frozen evidence, a recomputed component, or a final transcript
value, so at least one mandatory comparison fails.
\end{proof}

The CAP trace uses 389,974 Anemoi permutations.  Binding the 32-byte message to
the actual 5,391-byte commitment requires one further XOF and 58 permutations.
Thus the complete reference path through $H_{\RBBC}$ has 122,848 XOF calls and
390,032 Anemoi permutations.  At 336 nonlinear rows per permutation, the CAP
subtotal is 131,031,264 rows and request binding adds 19,488, for 131,050,752
permutation rows in this end-to-end path.  The ordered trace digest below covers the
122,847 CAP calls; the request-binding input length, message digest, output, and
58-permutation accounting are recorded separately in the same document.

The frozen linked-document, commitment, trace, and request digests are,
respectively,
\begin{center}
\texttt{a7ef7eb4689d84c686074308d0d167e1567ab84578d66b5ad004970940ea0163},\\
\texttt{12123e95b1b64d87da7a575dbf803fc53ded9eb9a23b807eeab2cba51dfe5b62},\\
\texttt{ccfa51ec2aee9501483c65023c4a877316eb6dd0557ccd6c42dfdf5f20f2c4e6},\\
\texttt{3f9ec0aeab100e4ebef8046068851874f08fcda6daa2e421},\\
\texttt{78dd559f55e38a31da28af9ccd0653bb4ca574ec8264cce1f},\\
\texttt{3c024c97e858e2c877bba7968c039dc61f516dfed995ba3}.
\end{center}
The last value is 72 bytes and is wrapped in the source only for typography.

Adding all 18 verified one-tree fixture envelopes gives 398,032,312 wires,
390,142,528 nonlinear rows, 132,327,002 linear rows, 522,469,530 total rows,
377,830,939,120 virtual row-stream bytes, and 9,950,810,104 assignment-archive
bytes.  These are conservative resource-envelope numbers, not the exact count
of a deduplicated production circuit: every one-tree fixture contains its own
inputs, $H_1$/points, $H_2$/commitment, and request tail.  A native composition
must retain the tree-local constraints while sharing one global tail and
constraining every cross-segment port.  The linked SHA-256 records are
reproducibility evidence; they are not a substitute for those rank-one rows or
for a complete assignment replay.

\paragraph{Shared production global tail in version 2.9.}
The version-2.8 schedule determines a single consumer-side port ABI.  It has
one 386-bit shared-salt port, one 256-bit request-message port, and, for each
tree in profile order, ports for leaf commitments, the plain witness
polynomial, the plain consistency polynomial, and the extension-field
$\xi$ masks.  All port bits are ordinary Boolean-constrained native wires.

\begin{proposition}[Assignment-backed shared-tail closure]
For the frozen production execution, relation
\texttt{pq-rbbc/cap/production-global-tail/v1} has 56,806,711 rows and
40,194,596 wires.  Its canonical row-stream SHA-256 is
\begin{center}\small
\texttt{c368d41de9e57910803e98284a4ec0a0}\\
\texttt{f45862fa80f3d070a50477f82627c9df}.
\end{center}
Its 1,004,865,028-byte fixed-width assignment archive has SHA-256
\begin{center}\small
\texttt{946c1feef78741f0b7e04cfffb237c08}\\
\texttt{b4736d63d8214f8202f822da6e8ec8c1}.
\end{center}
A complete mmap replay has
zero failed rows and zero external assertions.  Mutating a tree-commitment
source, correction source, $\alpha$ packing row, $H_2$ $\xi$ source,
commitment-publication row, or request-digest packing row is rejected.
\end{proposition}
\begin{proof}
The row generator allocates every port in a fixed profile-order wire range and
enforces bitness before using it.  The correction rows link tree-zero plain
values to every other tree.  Native Horner multiplication and bit decomposition
derive the shared $\alpha$ from the $H_1$ points.  A single ordered sponge
lowering then binds the correction transcript, all eighteen $\xi$ components,
the canonical serialization, and the request message.  Generation writes all
field-wire values in allocation order; replay regenerates the witness-independent
topology, resolves each linear form from the mmap archive, and evaluates every
rank-one row.  The six probes each change a wire used by the named equality or
packing row while retaining the stale remainder of the assignment, hence that
row fails.  The frozen commitment and request outputs equal the canonical
version-2.8 reference values.
\end{proof}

This proposition closes only the shared consumer tail.  The existing one-tree
archives include their own duplicated global tails and cannot be relabelled as
the producer segments for this relation.  Version 2.9 therefore keeps
\path{tree_producer_segments_materialized},
\path{cross_segment_wire_identity_closed}, and
\path{complete_18_tree_assignment_replayed} false.

\paragraph{Reduced position-sensitive tree producers in version 2.10.}
For tree position $i$, the producer relation accepts the shared salt, the two
roots assigned to $i$, and the profile consistency points.  Tree index $i$ is
retained in every GGM and leaf metadata field.  The relation publishes four
contiguous Boolean-constrained ports named exactly as the shared-tail
consumers: leaf commitments, $P_i$, $\widehat M_i$, and the flattened
extension-field $\xi$ masks.

\begin{proposition}[Reduced producer assignment and port-ABI closure]
For each of the two four-leaf, degree-3 reduced trees, relation
\path{pq-rbbc/cap/tree-producer-segment/v1} has 34,148 rows and 23,329 wires.
The fixed-width assignment is 583,353 bytes and a complete replay has zero
failures and zero external assertions.  Six stale-witness probes per tree
reject.  All eight producer output ports match the version-2.9 reduced-tail
consumer ports by canonical ID, bit width, and value digest.
\end{proposition}
\begin{proof}
The generator allocates Boolean inputs for salt, roots, and consistency
points, then lowers only tree-local XOF calls in their actual profile position.
A temporary wire spool retains the selected tape-output wire IDs.  Wide XOR
forms publish $P_i$ and $\widehat M_i$; native Horner rows at the supplied
points and extension-field leaf selection publish the $\xi$ masks.  The mmap
verifier regenerates the same witness-independent topology and evaluates every
row.  Selected mutations cover GGM source binding, tape packing, and each of
the four output-port publications.  Finally, the port validator compares the
producer and tail ID, width, and canonical value digest for every output.
\end{proof}

This is not a native wire join.  Producer-local and tail-local wire IDs differ,
and a different valid consistency-point input is not rejected by the producer
alone.  The next relation must expose the tail $H_1$ points as phase-A outputs
and either share or explicitly constrain those exact wires before any
cross-segment identity claim becomes true.  The reduced fixture is
deliberately non-secure and supplies no production CAP claim.

The earlier 240/336/360 discrepancy remains documented, but it no longer
blocks implementation: version 2.0 intentionally chooses the 336-row profile.
The cost of that choice is loss of bit-exact Blind-UOV compatibility and loss
of automatic inheritance of its security and signature-size claims.

For historical comparison, version 1.9 used an illustrative paper row as its
anchor.  The Shockwave/Anemoi Blind-UOV-Is/Shorter result is reported as
approximately 16 million R1CS
constraints, a 2.8~MB proof, 32~s proving, and 2.5~s verification.  The paper
does not report the corresponding Blind-UOV-III row.  Scaling the native
constraint anchor by the witness-width ratio
$(576+1472)/(256+640)=2.2857$ gives a rough 36.57-million-row native baseline;
after the exact 684,419-row non-overlapping ticket/trace increment below, the
total is about 37.26 million.  The extra 1,152 bitness rows in the portable
v2.0 archive are already part of a genuine native $\pi_1$ relation and are not
double-counted in this combined estimate.
This is an extrapolation, not a paper result or local benchmark, and it is not
a version-2.0 fork estimate.  The fork changes the permutation constraint count
and has not yet materialized the complete CAP circuit.  The paper
explicitly treats these as estimates and notes that the benchmarked
implementations do not provide the required zero-knowledge mode.  It also does
not establish the simulation-extractability required by our concurrent GCCA
proof.  Therefore Shockwave/Anemoi is a \emph{costing backend}, not yet the
security-frozen $\Piiss$ implementation~\cite{blind-uov}.

The multivariate commitment
$c=(\mu+F(r),G(r))$ and relation proofs of Bouillaguet et al.
are a promising later optimization because they are algebraically aligned with
UOV, but they add a newer MQ/bilinear-system assumption and are not the baseline
for the first auditable construction~\cite{mq-commitments}.

\subsection{Threshold Niederreiter for trace encryption}

\paragraph{Code and plaintext profile.}
Let $\mathcal G$ be a binary Goppa code of length $n$, dimension $k$, and
designed error weight $t$, with public systematic parity-check matrix
$H_{\mathrm{pub}}=(I_{n-k}\mid T)$.  The selected decoder MUST guarantee
$d_{\min}(\mathcal G)\geq 2t+1$.  Fix
$\rid\in\{0,1\}^{256}$ and $\sn\in\{0,1\}^{128}$, and define the fixed
48-byte plaintext
\[
  \mu=\mathsf{Encode}_{48}(\rid\|\sn).
\]
The online ciphertext uses a weight-$t$ error $e\in\mathbb F_2^n$ as its only
encryption witness.  Thus the abstract randomness $\rho_{\mathrm{tr}}$ is
instantiated by $e$.

\paragraph{Hash DEM.}
Sample $e$ uniformly from the weight-$t$ vectors and compute
\begin{align}
 s&=H_{\mathrm{pub}}e^\top\in\mathbb F_2^{n-k},
   &\mathrm{wt}(e)&=t, \tag{N1}\label{eq:n1}\\
 \mathsf{ad}&=\mathsf{Encode}(\ctx,\sn,h), &&\tag{N2}\label{eq:n2}\\
 Z&=\mathsf{SHAKE256}(
   \mathtt{PQ\mbox{-}RBBC/KDF}\|e\|s\|\ctx,640),
   &&\tag{N3}\label{eq:n3}\\
 (K_{\mathrm{mac}},P)&=Z,
   &|K_{\mathrm{mac}}|&=32,\quad |P|=48, \tag{N4}\label{eq:n4}\\
 c_\mu&=\mu\oplus P, &&\tag{N5}\label{eq:n5}\\
 \gamma&=\mathsf{KMAC256}_{K_{\mathrm{mac}}}
 (s\|c_\mu\|\mathsf{ad},256;
   \mathsf{custom}=\mathtt{PQ\mbox{-}RBBC/TAG}),
 &&\tag{N6}\label{eq:n6}\\
 C&=(s,c_\mu,\gamma). &&\tag{N7}\label{eq:n7}
\end{align}
The output length in (N3) is in bits.  SHAKE256 and KMAC256 are the standardized
extendable-output and MAC functions of FIPS~202 and SP~800-185,
respectively~\cite{fips202,sp800185}.  The composition $\HDEM$ is nevertheless a
new protocol profile, not a standardized AEAD and not yet a production claim.
A standardized one-time AEAD may replace it later without changing the abstract
$R_{\mathsf{enc}}$ interface.

\begin{definition}[Concrete trace relation]\label{def:ntr}
For $C=(s,c_\mu,\gamma)$, define
\[
 R_{\mathsf{Ntr}}(\mathsf{tpk},C,\ctx,h;
                    \rid,\sn,e)=1
\]
iff $\mathsf{tpk}$ selects $H_{\mathrm{pub}}$ and the fixed profile above, and
Equations~\eqref{eq:n1}--\eqref{eq:n7} all hold with
$\mu=\mathsf{Encode}_{48}(\rid\|\sn)$.  In this version,
\[
 \begin{aligned}
 &R_{\mathsf{enc}}(\mathsf{tpk},C,
     \mathsf{Encode}(\ctx,\sn,h);\rid\|\sn,\rho_{\mathrm{tr}})=1\\
 &\qquad\Longleftrightarrow\quad
 R_{\mathsf{Ntr}}(\mathsf{tpk},C,\ctx,h;\rid,\sn,e)=1,
 \qquad \rho_{\mathrm{tr}}=e.
 \end{aligned}
\]
Hence $\mathcal C_{\mathsf{trace}}$ is now the exact conjunction (N1)--(N7),
not an unspecified ``encrypt correctly'' box.
\end{definition}

\begin{lemma}[Unique syndrome witness]\label{lem:unique-error}
For a code with $d_{\min}\geq2t+1$, a syndrome $s$ has at most one witness
$e$ of weight at most $t$ satisfying $s=H_{\mathrm{pub}}e^\top$.
\end{lemma}

\begin{proof}
If $e$ and $e'$ were two such witnesses, then
$d=e+e'$ would satisfy $H_{\mathrm{pub}}d^\top=0$ and
$\mathrm{wt}(d)\leq2t$.  If $e\neq e'$, $d$ is a nonzero codeword below the
minimum distance, a contradiction.  Therefore $e=e'$.  This is the standard
unique-decoding argument used by Classic McEliece~\cite{classic-mceliece-spec}.
\end{proof}

\begin{corollary}[Unique trace opening]\label{cor:unique-trace}
The concrete relation $R_{\mathsf{Ntr}}$ has information-theoretically unique
plaintext openings.
\end{corollary}

\begin{proof}
For a fixed $C$, Lemma~\ref{lem:unique-error} fixes $e$ from $s$.  Equations
(N3)--(N4) then deterministically fix $P$ and $K_{\mathrm{mac}}$, and (N5)
fixes the sole $\mu=c_\mu\oplus P$.  The fixed-width encoding is injective, so
it fixes $(\rid,\sn)$.  Equation~(N6) additionally rejects any inconsistent
associated data.  No computational assumption is needed for uniqueness.
\end{proof}

\begin{proposition}[Concrete trace correctness]\label{prop:ntr-correct}
If threshold decoding reconstructs the unique error $e$ for $s$, the combiner
recomputes (N3)--(N6), returns $\mu=c_\mu\oplus P$, and accepts only if
$\gamma$ verifies and the decoded serial equals the visible $\sn$.
Consequently every honestly formed concrete trace ciphertext opens to exactly
$\rid\|\sn$.
\end{proposition}

\paragraph{Privacy assumption and proof boundary.}
Let $\mathsf{t\text{-}OW\text{-}SD}$ be the game in which an adversary receives
$H_{\mathrm{pub}}$, fewer than $\tau$ threshold shares, and
$s=H_{\mathrm{pub}}e^\top$ for uniform weight-$t$ $e$, and must output $e$.
We assume this advantage is negligible for the frozen distributed decoder.

\begin{theorem}[Conditional concrete trace privacy]\label{thm:ntr-privacy}
Model SHAKE256 as a quantum random oracle, assume KMAC256 is a quantum-secure
PRF, never reuse $e$, and let $\epsilon_{\mathsf{O2H}}(q_H)$ denote the loss of
the applicable one-way-to-hiding lemma for $q_H$ quantum hash queries.  Then for
every fewer-than-threshold $\QPT$ distinguisher $\mathcal A$ there are $\QPT$
reductions $\mathcal B_1,\mathcal B_2$ such that
\begin{align*}
 \Adv^{\mathsf{t\text{-}IND\text{-}CPA}}_{\mathsf{Ntr},\mathcal A}
 \leq{}&
 \Adv^{\mathsf{t\text{-}OW\text{-}SD}}_{\mathcal G,\mathcal B_1}
 +\epsilon_{\mathsf{O2H}}(q_H)
 +\Adv^{\mathsf{qPRF}}_{\mathsf{KMAC256},\mathcal B_2}.
\end{align*}
\end{theorem}

\begin{proof}
Starting from the real game, replace the 640-bit SHAKE256 value at the hidden
input $(e,s,\ctx)$ by a uniform string.  A distinguisher for this step
either yields a threshold syndrome-decoding solver or is bounded by the stated
one-way-to-hiding loss.  Now $P$ is an independent uniform 48-byte pad and
$K_{\mathrm{mac}}$ is an independent uniform key.  Replace KMAC256 under that
key by a random function, paying the qPRF term.  The value
$c_\mu=\mu_b\oplus P$ and the tag are then independent of $b$, so the final
advantage is zero.  Summing the hybrid differences gives the bound.
\end{proof}

A new accepted tuple $(s,c'_\mu,\gamma')$ with the same hidden derived key and
different authenticated input gives a KMAC forgery, up to the same
syndrome-decoding/QROM bad event.  Thus the xor pad gives one-time privacy and
KMAC256 gives one-time ciphertext integrity.  Substituting
Theorem~\ref{thm:ntr-privacy} for the abstract $\tau$-IND-CPA term in
Theorem~\ref{thm:gcca} yields protocol-level $\tau$-GCCA, plus the SE-AoK,
forked blind-signature, CAP, unique-opening, authorization, and hash terms already displayed
there.  This does \emph{not} prove standard IND-CCA security for a bare
ciphertext endpoint.

The threshold-Niederreiter work solves malformed-ciphertext attacks by attaching
a straight-line-extractable validity proof to every public ciphertext; its
authors report about 30.5~KB for a three-party ciphertext, with the proof
dominating the syndrome~\cite{threshold-niederreiter,threshold-niederreiter-slides}.
Here the same validity language
$\{s:\exists e,\mathrm{wt}(e)=t,\;s=He^\top\}$
is certified once inside $\pi_{\mathsf{issue}}$.  The online ticket retains only
$C=(s,c_\mu,\gamma)$, and the raw distributed decoder remains unreachable.

\paragraph{Exact online byte counts.}
Because $|c_\mu|=48$ and $|\gamma|=32$, the trace ciphertext is exactly the
syndrome length plus 80 bytes.  Using the published Classic McEliece
implementation sizes gives the following anchors~\cite{classic-mceliece-impl}:
\begin{center}
\begin{tabular}{lrrr}
\toprule
Parameter anchor & Public key & Syndrome & This $C_{\mathrm{trace}}$\\
\midrule
mceliece348864  & 261120 B  &  96 B & 176 B\\
mceliece460896  & 524160 B  & 156 B & 236 B\\
mceliece6688128 & 1044992 B & 208 B & 288 B\\
mceliece6960119 & 1047319 B & 194 B & 274 B\\
mceliece8192128 & 1357824 B & 208 B & 288 B\\
\bottomrule
\end{tabular}
\end{center}
The aggressive working anchor is mceliece6688128: approximately 1.045~MB of
cached public key buys a 288-byte trace ciphertext.  The 368-byte payload is
exact.  A 12,012-byte complete ticket results only if the fork reproduces the
paper's 11,644-byte signature target; version 2.4 has not established that
condition.

\begin{remark}[Mandatory 2026 cryptanalytic hold]\label{rem:mceliece-hold}
The anchor above is not frozen.  A 10 August 2026 preprint gives a classical
quasipolynomial-time distinguisher applying to all Classic McEliece NIST
parameter sets and extends it to a heuristic quasipolynomial-time decryption
attack; its concrete improvements are described as not yet practical
\cite{mceliece-2026-attack}.  That is not a demonstrated practical break, but it
materially changes confidence.  Deployment parameter selection is blocked
until independent review quantifies this attack for the threshold construction.
\end{remark}

\subsection{Reproducible five-block circuit audit}\label{sec:circuit-audit}

\paragraph{Counting model.}
The paper's Anemoi profile uses an R1CS over $\mathbb F_{2^{129}}$ for NIST
level I.  Addition, xor, multiplication by public constants, fixed-width
concatenation, and the public binary syndrome map are linear.  Each Boolean AND
or field multiplication contributes one nonlinear R1CS constraint.  Following
the Blind-UOV paper, one Keccak-f permutation costs
$24\cdot1600=38{,}400$ constraints~\cite{blind-uov}.  The audit counts only
constraints added beyond a native well-built-request $\pi_1$ relation.  The
complete PQ-RBBC-BUOV-336 CAP circuit is not yet materialized, so the
historical paper-anchored level-III baseline below is an explicit version-1.9
comparator, not a version-2.8 fork estimate.

\paragraph{Exact-weight gadget.}
The witness $e$ has 6,688 bits.  Pad it with 1,504 public zeros to 8,192 inputs
and sum them using a fixed balanced binary adder tree.  Combining two $j$-bit
counters uses one half-adder and $j-1$ full adders.  Since xor is linear, the
nonlinear cost is $1+2(j-1)=2j-1$.  The exact cost is therefore
\[
 \sum_{j=1}^{13}\frac{8192}{2^j}(2j-1)=24{,}547
\]
AND constraints.  Equating the final 14-bit count to 128 is linear.  This
gadget avoids the invalid shortcut $\sum e_i=128$ in characteristic two, which
would check only parity.  Separately, $e$, $\sn$, and $k_{\mathrm{hold}}$
require $6688+128+256=7{,}072$ bitness constraints.

\paragraph{Exact Keccak accounting.}
SHAKE256 has a 136-byte rate.  With pad10*1, an $L$-byte message uses
$\lfloor L/136\rfloor+1$ absorb permutations.  The fixed encodings give:
\begin{center}
\begin{tabular}{lrrr}
\toprule
Invocation & Absorbed bytes & Keccak-f calls & Nonlinear constraints\\
\midrule
$H_{\mathrm{ticket}}(M)$ & $14+368=382$ & 3 & 115,200\\
$H_{\mathrm{hold}}(k)$ & $12+32=44$ & 1 & 38,400\\
Trace SHAKE KDF & $11+836+208+32=1087$ & 8 & 307,200\\
KMAC256 & $136+136+336+3=611$ & 5 & 192,000\\
\midrule
Total & & 17 & 652,800\\
\bottomrule
\end{tabular}
\end{center}
For KMAC, the first 136-byte block is the cSHAKE function/customization prefix,
the second is $\mathsf{bytepad}(\mathsf{encode\_string}(K),136)$, the
authenticated data is $208+48+80=336$ bytes, and
$\mathsf{right\_encode}(256)$ is three bytes.  All squeezing fits in the first
rate block and adds no permutation.

\paragraph{Five-block result.}
The incremental nonlinear counts are:
\begin{center}
\begin{tabular}{lrl}
\toprule
Block & Constraints & Contents\\
\midrule
$\mathcal C_{\mathsf{shape}}$ & 128 & serial bitness; widths/wiring linear\\
$\mathcal C_{\mathsf{ticketHash}}$ & 115,200 & three SHAKE permutations\\
$\mathcal C_{\mathsf{RBBCmask}}$ & 1,152 & bitness of $r,h_{\RBBC}$; 576 mask equations linear\\
$\mathcal C_{\mathsf{holder}}$ & 38,656 & 256 bitness + one SHAKE permutation\\
$\mathcal C_{\mathsf{trace}}$ & 530,435 & $e$ bitness, weight, KDF, and KMAC\\
\midrule
Portable v2.8 relation & 685,571 & includes 1,152 native-overlap hardening rows\\
\bottomrule
\end{tabular}
\end{center}
Of the portable total, 1,152 bitness rows protect the explicit $r$ and
$h_{\RBBC}$ interface while the full native generator is absent.  A genuine native
$\pi_1$ already constrains those binary values, so only 684,419 rows are
non-overlapping additions to the paper-anchored native baseline.  The 576
$y=r+h_{\RBBC}$ equations are linear.
The syndrome equation adds 1,664 linear output equalities but no nonlinear
gate.  The xor pad, associated-data construction, external/internal serial
equality, and final constant comparisons are also linear.

\paragraph{Historical paper-anchored issuance estimate.}
The level-III native baseline is estimated from the paper's rounded
16-million-constraint NIST-I figure by the witness-width ratio:
\[
 N_{\mathsf{native,III}}\approx16{,}000{,}000
 \frac{576+1472}{256+640}=36{,}571{,}429.
\]
Adding the exact incremental relation gives
\[
 N_{\pi_{\mathsf{issue}}}\approx36{,}571{,}429+684{,}419
 =37{,}255{,}848,
\]
or 2.3285 times the NIST-I anchor.  Applying the paper's square-root
proof-size scaling and linear time scaling gives a rough 4.27~MB proof,
74.5~s proving, and 5.82~s verification.  None of these level-III figures is a
paper result or local benchmark; the scaling may omit parameter-dependent
costs.  Peak prover memory remains a measurement obligation.

\paragraph{Version-2.1 CAP lower component count.}
The direct fork accounting supersedes the historical 36.57-million-row CAP
anchor as an engineering estimate.  Its sponge calls alone require
131,031,264 nonlinear permutation rows before extension-field arithmetic,
packing, correction, and equality rows are added.  Adding only the exact
684,419 non-overlapping ticket/trace increment already reaches 131,715,683
rows, still excluding those omitted CAP rows.  Therefore the earlier 37.26
million, 4.27~MB, and 74.5~s extrapolations are retained only as historical
paper comparisons and must not be used to budget this fork.  The direct profile
is aggressive in online bytes but computationally heavy for offline
$\pi_{\mathsf{issue}}$.

\begin{proposition}[Provisional monolithic-circuit feasibility decision]
Under the historical version-1.9 costing model above, adding the entire ticket
and trace relation to the estimated well-built-request NIZK changes its
baseline by about 1.87\%.  This percentage must be recomputed after the full
forked CAP circuit exists.  Until then, there is no demonstrated
communication-based reason to split
$R_{\mathsf{aug}}$ into linked proofs at this stage.  A split is justified only
if the selected simulation-extractable backend, implementation memory, or
prover parallelism makes the monolithic circuit impractical.
\end{proposition}

\paragraph{Exact payload and provisional complete-ticket target.}
For mceliece6688128, $|C|=288$ bytes and
\[
 |M|=32+16+32+288=368\text{ bytes}.
\]
Combining this with the published 11,644-byte Blind-UOV-III/Shorter signature
as a provisional fork target gives
\[
 |T|=|M|+|\sigma|=12{,}012\text{ bytes}
\]
before transport framing.  This is conditional arithmetic, not a measured
version-2.8 signature size.  Under the same provisional assumption, cached
public material is approximately
1,044,992 bytes for the trace key plus the paper's rounded 189.2~KB Blind-UOV
verification key.  Relative to a 70~KB online credential,
the target is about 5.83 times smaller.  The roughly 4.27~MB issuance-NIZK
figure is a superseded historical comparator; version 2.8 already counts more
than 131 million CAP permutation rows.  Both the CAP commitment and
$\pi_{\mathsf{issue}}$ remain offline and are never forwarded to a satellite
verifier.

\begin{remark}[Audit reproducibility]
The companion executable relation recomputes every exact incremental integer
above and records it in the version-2.8 manifest.  Fork signature and native
CAP estimates
are kept in separately labelled fields so a rounded extrapolation cannot be
mistaken for a benchmark.
\end{remark}

\subsection{Executable reference relation and rejection tests}
\label{sec:executable-relation}

Version 2.8 preserves and audits the incremental relation in
\texttt{pq\_rbbc\_reference.py}.  The witness builder derives
$h$, $s$, $c_\mu$, $\gamma$, the canonical 368-byte payload, and its ticket
digest from $(\rid,\ctx,\sn,k_{\mathrm{hold}},e)$.  The circuit builder then
emits a stream of characteristic-two events of four kinds: linear wire
definitions, linear equality assertions, Boolean multiplication gates, and
bitness constraints $x(x+1)=0$.  A backend can consume the same stream without
retaining the whole circuit in memory.  This version executes the stream and
counts it, but does not yet flatten it into a proof system's matrix format.

The symbolic Keccak-f implementation performs all 24 rounds.  Its SHAKE256
output is checked against Python's independent FIPS~202 implementation, and
the SP~800-185 KMAC256 output is checked against the OpenSSL 3 KMAC provider
\cite{fips202,sp800185}.
For the frozen honest vector the generated circuit reports:
\begin{center}
\begin{tabular}{lr}
\toprule
Measured generator quantity & Value\\
\midrule
Allocated wire identifiers & 2,980,304\\
Linear definitions & 2,282,475\\
Linear equality assertions & 3,534\\
Nonlinear constraints & 685,571\\
Public input bits & 4,032\\
Secret input bits & 8,224\\
External forked CAP-hash assertions & 1\\
Keccak-f permutations & 17\\
Failed assertions & 0\\
\bottomrule
\end{tabular}
\end{center}
Thus the executable result exactly reproduces every nonlinear block total in
the preceding audit.  The linear-definition figure is diagnostic IR size, not
an additional nonlinear count: the costing model requires a backend to inline
affine expressions.  A backend that instead materializes each affine wire as
an R1CS row must report that larger total explicitly.  On the present reference
environment, one honest streaming generation takes about six seconds.  This is Python relation
generation time, not NIZK proving time, and no prover-memory claim follows from
it.

The regression suite accepts the honest witness and sends eleven independent
mutations through the complete circuit: wrong error weight, changed syndrome,
changed masked identity, changed holder hash, changed KMAC tag, changed serial,
changed blind request, changed blind mask, changed $H_{\RBBC}$ image, changed
CAP randomness, and changed common context.  All eleven are rejected.
The checked output is recorded in
\path{pq_rbbc_reference_manifest_v2_2.json}.

\paragraph{Exact implementation boundary.}
The $H=(I\mid T)$ matrix in the reference vector is generated deterministically
from a public seed and is only a rank-controlled test fixture; it is not a
certified binary Goppa key.  Similarly,
\texttt{TestPQRBBC336Adapter} uses the selected Anemoi sponge for
$H_{\RBBC}$ but derives a 48-byte commitment with a labelled SHAKE test
fixture.  It is not a blind signature or production CAP.  The new CAP module
and strict API separately test the production serialization and byte join, but
this incremental relation still marks one native full-row CAP-plus-hash
boundary.  The 576 public $y$ bits and
1,152 secret $r,h_{\RBBC}$ bits are serialized as circuit inputs; $m$ is derived internally,
and $\rho,c_r$ remain hidden behind the native boundary.  Consequently this implementation establishes that the incremental
relation is internally consistent and executable; it does not establish that
the combined native fork proof is zero knowledge or simulation
extractable.

\subsection{Portable binary R1CS lowering}\label{sec:br1cs}

Version 2.3 preserves the proof-backend exchange layer in
\texttt{pq\_rbbc\_br1cs.py}.  It consumes the same event stream as the counting
sink and serializes three standard rank-one forms over $\mathbb F_2$:
\begin{align*}
  \mathsf{MUL}(a,b,c)&:\quad a\cdot b=c,\\
  \mathsf{LINDEF}(c;x_1,\ldots,x_j;k)&:\quad
       (c+x_1+\cdots+x_j+k)\cdot 1=0,\\
  \mathsf{ASSERT}(x_1,\ldots,x_j;k)&:\quad
       (x_1+\cdots+x_j+k)\cdot 1=0.
\end{align*}
Wire identifiers and arities use unsigned LEB128 encodings.  A fixed 124-byte
header commits to all row counts, the body length, and the SHA-256 digest of the
body.  Public and secret input declarations contain identifiers and visibility
only; assignment values are not embedded in the circuit archive.

The resulting honest archive has the following exact dimensions:
\begin{center}
\begin{tabular}{lr}
\toprule
Portable backend quantity & Value\\
\midrule
Wires & 2,980,304\\
Public input bits & 4,032\\
Secret input bits & 8,224\\
Nonlinear R1CS rows & 685,571\\
Materialized affine-definition rows & 2,282,475\\
Linear assertion rows & 3,534\\
Total R1CS rows & 2,971,580\\
Archive size & 49,227,687 bytes\\
\bottomrule
\end{tabular}
\end{center}
The 685,571 figure is therefore a \emph{nonlinear-only} cost under the affine
elimination model used in Section~\ref{sec:circuit-audit}.  The 2,971,580 figure
is the portable total when every affine definition is materialized as its own
rank-one row.  They are not competing measurements.  A concrete proof backend
may recover the smaller representation by substituting affine expressions into
multiplication rows, but that optimization requires its own soundness argument
and resource benchmark.

Although the exchange format names $\mathbb F_2$ as its base relation, the same
Boolean assignment and equations embed into $\mathbb F_{2^d}$: addition remains
xor, and the input bitness equations prevent non-Boolean witnesses.  This makes
the archive compatible in principle with both the $\mathbb F_{2^{129}}$
costing anchor and the target $\mathbb F_{2^{193}}$ level-III field, without
claiming that the required lift or a specific prover has been integrated.

An independent reader reconstructs and evaluates all 2,971,580 rows.  It
accepts the honest assignment, rejects a one-bit assignment mutation, and
rejects a one-bit archive corruption through the body digest.  Lowering a
wrong-weight witness produces exactly the same 49,227,563-byte body and the
same digest as the honest witness, showing that circuit structure is
witness-independent; its assignment nevertheless fails 320 rows.  Five
end-to-end backend tests pass; wall time is environment-dependent.  The checked
metadata is recorded in
\texttt{pq\_rbbc\_br1cs\_manifest\_v2\_7.json}.

\paragraph{Remaining external rows.}
The archive records one named external assertion for the eighteen native
tree-producer segments, their exact cross-segment identities into the now
closed shared tail, and the final $H_{\RBBC}$ parent wire join.  Direct
reference evaluation, shared-tail replay, and the canonical byte join exist,
but the archive cannot yet evaluate those operations as one native
witness-independent row stream.
Version 2.10 narrows the engineering uncertainty by executing the producer
architecture on the reduced fixture; it does not remove any part of this
production external assertion.
The linear equation $y=r+h_{\RBBC}$ is no longer external.  This narrowed
boundary is deliberate and machine-visible: an archive with an unchecked
external assertion is not a closed issuance proof.  The next integration must
replace that marker with the forked CAP.Commit constraints and exact sponge
wire identities; deleting or
treating it as automatically true is forbidden.

\subsection{Local mixing as a future trace-encryption candidate}

The core does not depend on elliptic curves, lattices, or codes.  A future
local-mixing encryption may replace threshold Niederreiter only if it supplies
all four properties (T1)--(T4), an efficiently provable NP encryption relation,
and a practical $\tau$-party opening protocol.  A small standalone ciphertext
claim is insufficient by itself: threshold opening, unique opening, and
extractable issuance binding are the missing system properties.

\section{What has and has not been proved}

\begin{longtable}{p{0.31\linewidth}p{0.61\linewidth}}
\toprule
\textbf{Status} & \textbf{Result or obligation}\\
\midrule
\endfirsthead
\toprule
\textbf{Status} & \textbf{Result or obligation}\\
\midrule
\endhead
\bottomrule
\endfoot
Conditional reductions & Correctness; proof/request/message binding;
certified ciphertext validity; signature-gated decoder safety; issuer
unlinkability against an honest-protocol curious issuer; trace soundness;
one-more ticket unforgeability; and $\tau$-GCCA trace privacy.\\
\addlinespace
Assumed component property & Post-quantum SE-NIZK AoK; CAP straightline
extraction and unique committed mask; QROM cross-message request binding;
PQ-RBBC-BUOV-336 fork blindness and one-more
unforgeability; threshold syndrome-decoding privacy with robust shares; QROM
one-way-to-hiding; KMAC integrity; and
authorization unforgeability.\\
\addlinespace
Concretely instantiated & A 72-byte hidden-state request ABI; executable
five-block augmented relation; exact Niederreiter--SHAKE--KMAC trace relation;
information-theoretic unique opening; independently checked SHAKE/KMAC;
full-circuit positive and negative tests; an internally constrained
$y=r+h_{\RBBC}$ equation; a fail-closed fork-native import contract; a checksummed
portable R1CS archive with independent row evaluation; 685,571 incremental
nonlinear rows; 2,971,580 materialized total rows; a source-pinned
$\mathbb F_{2^{193}}$ 336-row Anemoi permutation; and an independent 772-bit
rate/772-bit capacity sponge with arbitrary-length native squeezing and a
576-bit request output.  The frozen two-block
hash trace has 2,238 rows and 2,235 wires; a direct CAP.Commit reference with
salted GGM trees, leaf commitments/tapes, corrections, and consistency hashing;
a canonical 5,391-byte production commitment encoding; a strict byte-level
CAP-to-$H_{\RBBC}$ join; an 88,282-row zero-callback reduced native trace; a
4,858-row standalone 2,450-bit tape trace; a 113,802-row extended CAP trace;
a 2,845-row production-width 11-coefficient Horner trace; a 125,401-row
two-coefficient CAP/Horner integration whose eight tapes have production width
and a bounded-memory 2,048-leaf production-shape stream with 26,126,283 rows,
19,903,324 wires, the full 2,048-bit witness, degree-12 masks, two points, and
2,450-bit tapes, plus a 497,583,228-byte fixed-width archive whose complete
assignment satisfies every row and rejects five stale-witness probes; and a
4,096-leaf, degree-13 stream with 52,224,501 rows, 39,789,564 wires, a
994,739,228-byte assignment archive, zero verification failures, and five
rejected stale-witness probes; plus a full 18-tree reference execution and
canonical linked schedule covering 122,847 CAP XOF calls, 17 correction pairs,
one 5,391-byte commitment, and five rejected stale-document probes (the shard
fixtures remain non-secure and the linked schedule is not a native assignment);
plus a version-2.9 shared global tail with 56,806,711 rows and 40,194,596 wires,
a completely replayed fixed-width assignment, zero external assertions, and
six rejected stale-witness probes; plus two version-2.10 reduced
position-sensitive producer segments with 34,148 rows and 23,329 wires each,
complete assignment replay, twelve rejected probes, and eight matching tail
port values.\\
\addlinespace
Not yet instantiated & A post-quantum zero-knowledge and
simulation-extractable implementation of $\Piiss$; a native
$\mathbb F_{2^{193}}$ materialization of all 18 tree-producer segments, exact
wire identities from those segments into the closed shared tail, and complete
composition replay; a fork-specific
blindness/one-more-unforgeability proof; fresh key, signature, and proof-size
benchmarks; validation of the CAP properties used by
Theorem~\ref{thm:j-qrom};
proof-system-specific affine elimination; import of a real Goppa
key; peak prover memory; robust threshold-share transcript; proof benchmarks;
and a reviewed response to the August 2026 McEliece cryptanalysis.\\
\addlinespace
Explicitly not claimed & Standard IND-CCA security of the bare ciphertext,
security against a malicious issuer, metadata/timing anonymity without system
controls, UC composability, side-channel resistance, satellite AKE security,
or FLEO/handover security.\\
\end{longtable}

\section{Implementation invariants}

The following invariants are part of the cryptographic design and should be
machine-tested or enforced by type/API boundaries.

\begin{enumerate}[label=INV-\arabic*,leftmargin=*]
  \item There is no production function that accepts a bare trace ciphertext
  and returns a partial decryption.
  \item The bytes hashed for the issuer signature are exactly the canonical
  bytes parsed by ticket verification and opening.
  \item The issuer signs only after successful proof verification and records a
  completed session only after returning its blind response.
  \item The blind request proven in $\Piiss$ is byte-for-byte the request passed
  to the blind-signing implementation.
  \item The public issuance request contains exactly the 72-byte value $y$; it
  contains neither $m$ nor $c_r$.  The mask and commitment randomness remain
  hidden and the commit-before-hash order is enforced.
  \item The opening authorization includes the ticket digest and evidence
  digest, and replay is statefully rejected.
  \item The reconstructed serial number must equal the clear serial number.
  \item Issuer keys and opening keys have independent generation ceremonies,
  storage, rotation, and compromise domains.
  \item Epoch metadata is federation-fixed; the issuer cannot personalize it.
  \item Every component and parameter set has an explicit post-quantum security
  claim at the chosen target category.
  \item The parser accepts exactly the frozen 368-byte payload layout and
  rejects variable-length or alternate encodings before signature verification.
  \item The issuance circuit enforces bitness of all 6,688 error coordinates
  and proves exact weight 128 with the specified counter gadget; a parity-only
  check is forbidden.
  \item The Shockwave/Anemoi figures may be used for costing only until a
  backend meeting the stated zero-knowledge and simulation-extractability model
  is proved or substituted.
  \item Production configuration rejects the deterministic parity-check matrix
  and test-only blind-signature adapter; neither fixture may cross the backend
  boundary.
  \item Backend lowering must preserve every multiplication and equality event,
  eliminate affine wires soundly, and accept/reject the published reference and
  mutation vectors identically to the executable IR.
  \item A serialized circuit is accepted only if its declared length, row
  counts, sequential wire discipline, and body digest all verify before proof
  generation or verification.
  \item Any nonzero external-assertion count makes the archive incomplete for
  production issuance; the PQ-RBBC-BUOV-336 marker must be replaced, never ignored.
  \item The 576 mask bits and 576 $H_{\RBBC}$-image bits are secret circuit
  inputs with bitness constraints; all 576 equations $y_j+r_j+h_{\RBBC,j}=0$
  must remain ordinary rows after lowering.
  \item A native import is accepted only if it matches the frozen
  $\mathbb F_{2^{193}}$ profile, has witness-independent topology and zero
  external assertions, and satisfies every check in the native-import contract.
  \item The selected sponge, version-2.1 CAP reference, version-2.2 reduced
  native trace, version-2.3 extended multi-squeeze trace, version-2.4 Horner
  relations, version-2.7 assignment-backed 2,048- and 4,096-leaf streamed
  shards, and the version-2.8 full-reference linked composition define
  the implemented
  $H_{\RBBC}$ wires unambiguously.  The CAP fixtures are explicitly non-secure
  and cannot satisfy the production import contract.  Version 2.9 additionally
  fixes and completely replays the exact shared global tail.  Version 2.10
  additionally fixes and replays the reduced producer-side port ABI.  The
  production producer segments, consistency-point and cross-segment wire
  identities, complete composition replay, and parent-circuit wire identities
  are still required.
  \item The fork must never claim bit-exact Blind-UOV compatibility.  The
  reported 240 rows remain comparison data; the selected fork uses 336 rows.
  \item Production closure requires independent fork vectors and cryptanalysis,
  a fork-specific blindness/one-more-unforgeability proof, and fresh key,
  signature, proof-size, time, and memory benchmarks.  The paper's 11,644-byte
  signature and the derived 12,012-byte ticket remain provisional targets.
\end{enumerate}

\section{Next proof step}

The independent 336-row permutation, canonical sponge framing, 576-bit
$H_{\RBBC}$ output, native hash rows, hidden-state adapter, direct CAP reference,
canonical 5,391-byte commitment, strict byte ABI, incremental relation, and
fail-closed fork contract are complete.  The reduced CAP fixture and final
$H_{\RBBC}$ join also have a zero-callback native row stream with zero external
assertions.  Generic native multi-squeeze, a frozen 2,450-bit tape, an extended
CAP integration, bit-bound native multiplication, production-width Horner
hashing, and a two-coefficient symbolic-mask CAP integration are complete as
well.  The full 2,048-bit witness has now been executed separately in both
production tree shapes: a 2,048-leaf degree-12 tree and a 4,096-leaf degree-13
tree.  Each has a frozen bounded-memory row-stream digest, a complete
  fixed-width assignment, whole-row verification, five stale-witness rejections,
  and zero external assertions.  Version 2.8 additionally executes the full
  production reference and freezes its canonical 18-tree linked schedule,
  corrections, commitment, and request transcript.  Version 2.9 materializes
  the shared consumer tail, replays its complete fixed-width assignment with
  zero failures, and rejects six selected stale witnesses.  Version 2.10
  implements and replays two reduced position-sensitive producer segments,
  rejecting twelve selected stale witnesses and matching all eight consumer
  ports by value.  The 240-row
artifact gap is no longer
an engineering blocker because the fork intentionally gives up bit-exact
compatibility.  The next component is exact native multi-tree composition:

\begin{quote}
\emph{Expose the shared tail's $H_1$ consistency-point wires as a phase-A
output port and prove split-tail equivalence.  Then materialize one actual
4,096-leaf/degree-13 and one actual 2,048-leaf/degree-12 producer without
duplicating the tail, freeze every point and output port-to-wire relocation,
expand to all eighteen positions, and replay the complete composition with
zero failures.  Then join the resulting
CAP bytes to the parent archive, which must reject changed $m$, $r$, $\rho$,
$c_r$, correction, serialization, or hash output and reach zero external
assertions.  Then prove
unique-mask and straightline extraction, revalidate blindness and one-more
unforgeability, benchmark the actual signature, and qualify a post-quantum
zero-knowledge and simulation-extractable backend.}
\end{quote}

\paragraph{Delivery roadmap.}
The companion file
\texttt{PQ\_RBBC\_CRYPTO\_CORE\_ROADMAP\_v2\_10.md} records the ordered gates:
(R1b-b/c) split-tail point ports, production producer segmentation, wire
relocation, and full assignment replay;
(R2) parent exact wire join and zero external assertions; (R3) fork-specific CAP, request-binding, blindness,
one-more-unforgeability, SE-NIZK, and QROM reductions; (R4)
signature-gated-decryption implementation and reduction; (R5) post-quantum
backend qualification and fresh size/performance benchmarks; and (R6)
satellite AKE, anti-replay, handover, revocation, and availability integration.
Production remains fail-closed until every gate is independently reviewed.

The 131-million-row permutation subtotal is large enough that monolithic versus
linked proof architecture must now be benchmarked; the earlier monolithic
preference is no longer justified by the historical 37-million-row estimate.
The Niederreiter deployment parameter MUST remain provisional under
Remark~\ref{rem:mceliece-hold}.  After backend qualification, the remaining
cryptographic component is the robust threshold-share transcript.  Satellite
AKE integration remains out of scope until those two obligations close.

\begin{thebibliography}{99}

\bibitem{blind-uov}
Charles Bouillaguet, Thibauld Feneuil, Jules Maire, Matthieu Rivain,
Julia Sauvage, and Damien Vergnaud,
\emph{Blinding Post-Quantum Hash-and-Sign Signatures},
IACR Cryptology ePrint Archive, Report 2025/895, 2025.
\url{https://eprint.iacr.org/2025/895}

\bibitem{anemoi-code}
Cl\'emence Bouvier et al.,
\emph{Anemoi: a Family of ZK-friendly Arithmetization-Oriented Hash
Functions}, reference Sage implementation.
\url{https://github.com/anemoi-hash/anemoi-hash}

\bibitem{anemoi-paper}
Cl\'emence Bouvier, Pierre Karpman, Cl\'ement Keller, Adeline Roux-Langlois,
Yann Rotella, and Marine Verbraeken,
\emph{New Design Techniques for Efficient Arithmetization-Oriented Hash
Functions: Anemoi Permutations and Jive Compression Mode},
IACR Cryptology ePrint Archive, Report 2022/840, 2022.
\url{https://eprint.iacr.org/2022/840}

\bibitem{conway-polynomials}
Frank Luebeck,
\emph{Conway Polynomials for Finite Fields: Characteristic 2}.
\url{https://www.math.rwth-aachen.de/~Frank.Luebeck/data/ConwayPol/CP2.html}

\bibitem{bht}
Gilles Brassard, Peter H\o yer, and Alain Tapp,
\emph{Quantum Algorithm for the Collision Problem},
arXiv:quant-ph/9705002, 1997.
\url{https://arxiv.org/abs/quant-ph/9705002}

\bibitem{compressed-oracle}
Kai-Min Chung, Serge Fehr, Yu-Hsuan Huang, and Tai-Ning Liao,
\emph{On the Compressed-Oracle Technique, and Post-Quantum Security of Proofs
of Sequential Work},
Advances in Cryptology---EUROCRYPT 2021; arXiv:2010.11658.
\url{https://arxiv.org/abs/2010.11658}

\bibitem{blind-uov-slides}
Thibauld Feneuil,
\emph{Blinding Post-Quantum Hash-and-Sign Signatures},
IEEE Symposium on Security and Privacy presentation slides, 2026.
\url{https://www.thibauld-feneuil.fr/docs/talks/2026-05-19_SP26_blind-sigs.pdf}

\bibitem{mq-commitments}
Charles Bouillaguet, Thibauld Feneuil, Jules Maire, Matthieu Rivain,
Julia Sauvage, and Damien Vergnaud,
\emph{Multivariate Commitments and Signatures with Efficient Protocols},
IACR Cryptology ePrint Archive, Report 2025/2035, 2025.
\url{https://eprint.iacr.org/2025/2035}

\bibitem{threshold-niederreiter}
Pascal Giorgi, Fabien Laguillaumie, Lucas Ottow, and Damien Vergnaud,
\emph{Threshold Niederreiter: Chosen-Ciphertext Security and Improved
Distributed Decoding},
IACR Cryptology ePrint Archive, Report 2025/757, 2025.
\url{https://eprint.iacr.org/2025/757}

\bibitem{threshold-niederreiter-slides}
Lucas Ottow,
\emph{Threshold Niederreiter: Chosen-Ciphertext Security and Improved
Distributed Decoding}, presentation slides, 2025.
\url{https://indico.math.cnrs.fr/event/11948/attachments/5546/9681/Lucas_Ottow.pdf}

\bibitem{classic-mceliece-spec}
Daniel J. Bernstein et al.,
\emph{Classic McEliece: conservative code-based cryptography}, specification.
\url{https://classic.mceliece.org/mceliece-spec-20221023.pdf}

\bibitem{classic-mceliece-impl}
Classic McEliece team,
\emph{Implementations and parameter sizes}.
\url{https://classic.mceliece.org/impl.html}

\bibitem{fips202}
National Institute of Standards and Technology,
\emph{FIPS 202: SHA-3 Standard}, 2015.
\url{https://csrc.nist.gov/pubs/fips/202/final}

\bibitem{sp800185}
National Institute of Standards and Technology,
\emph{SP 800-185: SHA-3 Derived Functions}, 2016.
\url{https://csrc.nist.gov/pubs/sp/800/185/final}

\bibitem{mceliece-2026-attack}
Ashrujit Ghoshal, Yuval Ishai, Aayush Jain, and Nuozhou Sun,
\emph{Quasipolynomial Cryptanalysis of the McEliece Cryptosystem
(or: PIR Meets McEliece)}, IACR Cryptology ePrint Archive,
Report 2026/1630, 2026.
\url{https://eprint.iacr.org/2026/1630}

\bibitem{blind-ciphertext-signatures}
Olivier Blazy, Georg Fuchsbauer, David Pointcheval, and Damien Vergnaud,
\emph{Signatures on Randomizable Ciphertexts},
Public Key Cryptography---PKC 2011.
\url{https://www.blazy.eu/Papers/2011_pkc.pdf}

\end{thebibliography}

\end{document}
