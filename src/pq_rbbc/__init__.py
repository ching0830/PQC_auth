"""System-level PQ-RBBC protocol packages.

Legacy cryptographic-core modules remain at ``src/pq_rbbc_*.py`` while their
artifact identities and long-running work are active.  New system modules use
this package hierarchy so that governance, opening, access, and handover code
can evolve behind explicit contracts.
"""
